% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_protein_impacts.R
\name{analyze_protein_impacts}
\alias{analyze_protein_impacts}
\title{Protein-Level Impact Table for a Mitogenome Comparison}
\usage{
analyze_protein_impacts(comparison, canonical = TRUE, mdp = TRUE)
}
\arguments{
\item{comparison}{A \code{mito_comparison} object returned by
\code{\link[=compare_mitogenomes]{compare_mitogenomes()}}.}

\item{canonical}{Logical.  Include canonical CDS proteins (default \code{TRUE}).}

\item{mdp}{Logical.  Include MDP ORFs (default \code{TRUE}).  Requires that
\code{scan_mdps = TRUE} was used in \code{\link[=compare_mitogenomes]{compare_mitogenomes()}}.}
}
\value{
A \code{data.frame} with one row per affected ORF and columns:
\describe{
\item{\code{orf_type}}{\code{"CDS"} or \code{"MDP"}.}
\item{\code{gene}}{Gene name for CDS; \code{NA} for MDP.}
\item{\code{strand}, \code{frame}}{Orientation of the ORF.}
\item{\code{orf_start}, \code{orf_end}}{Genomic coordinates in the reference.}
\item{\code{orf_length_aa}}{Reference protein length (aa, excluding stop).}
\item{\code{n_snps}, \code{snp_positions}}{Number and positions of contributing
mutations from the comparison.}
\item{\code{protein_ref}, \code{protein_alt}}{Amino acid sequences (stop stripped).}
\item{\code{n_aa_changes}}{Number of differing residue positions.}
\item{\code{aa_changes}}{Semicolon-separated list of substitutions, e.g.
\code{"T23A; Q45K"}.}
\item{\code{blosum_sum}}{Sum of BLOSUM62 scores across all substitutions
(more negative = more structurally disruptive).}
\item{\code{blosum_mean}}{Mean BLOSUM62 score per substituted position.}
\item{\code{physchem_changes}}{Semicolon-separated physicochemical class
changes, e.g. \code{"nonpolar→polar@23"}.}
\item{\code{n_charge_changes}}{Number of positions where charge class changed
(basic/acidic/neutral).}
\item{\code{cf_helix_ref}, \code{cf_helix_alt}, \code{cf_helix_delta}}{Mean Chou-Fasman
helix propensity for reference and alternate proteins, and their
difference.}
\item{\code{cf_sheet_ref}, \code{cf_sheet_alt}, \code{cf_sheet_delta}}{As above for
beta-sheet propensity.}
\item{\code{tm_ref}, \code{tm_alt}}{Whether a transmembrane helix is predicted
(Kyte-Doolittle window 19, threshold 1.6) in reference/alternate.}
\item{\code{tm_change_overlap}}{Logical: does any substitution fall within a
predicted TM region in the reference protein?}
}
}
\description{
Takes the output of \code{\link[=compare_mitogenomes]{compare_mitogenomes()}} and produces a companion table
with one row per affected protein (canonical CDS or MDP ORF).  For each
protein, the function aligns the reference and alternate amino acid sequences
and computes amino-acid-level structural metrics.
}
\details{
\subsection{Metrics}{

\strong{BLOSUM62} scores reflect evolutionary acceptability of each substitution;
large negative sums indicate radical changes unlikely to be structurally
tolerated.

\strong{Chou-Fasman propensities} (Chou & Fasman 1978) provide a residue-level
estimate of secondary-structure tendency.  \code{cf_helix_delta > 0} means the
alternate sequence has higher mean helix propensity.

\strong{Transmembrane prediction} uses a Kyte-Doolittle sliding window of 19
residues (threshold 1.6) and is only informative for proteins longer than
30 aa.
}

\subsection{MDP matching}{

Reference MDP ORFs that overlap a mutation position are matched to the
closest alternate ORF by strand, frame, and maximum protein-sequence
identity.  ORFs with no match above 40\% identity in the alternate genome
are reported as abolished (protein_alt = NA).
}
}
\examples{
\dontrun{
comp    <- compare_mitogenomes("NC_012920.1", "AP008907.1")
impacts <- analyze_protein_impacts(comp)
impacts[impacts$n_charge_changes > 0, ]
}

}
\seealso{
\code{\link[=compare_mitogenomes]{compare_mitogenomes()}}
}
