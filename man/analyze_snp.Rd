% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_snp.R
\name{analyze_snp}
\alias{analyze_snp}
\title{Analyze a Single Nucleotide Variant for Noncanonical ORF Impact}
\usage{
analyze_snp(
  variant,
  reference = "NC_012920.1",
  genetic_codes = .stop_no_code("genetic_codes"),
  min_orf_length = 60L,
  start_codons = c("ATG", "GTG", "ATA", "ATT", "ATC"),
  substitution_tolerance = 3L,
  rna_structure = FALSE,
  rna_flank = 100L,
  rna_strand = c("sense", "antisense"),
  rna_temperature = 37,
  rna_max_bulge = 2L
)
}
\arguments{
\item{variant}{Variant specification. Either a character scalar in
the form \code{"m.16189T>C"} / \code{"16189T>C"} / \code{"T16189C"}, or a list
with elements \code{pos} (integer), \code{ref} (character), \code{alt} (character).}

\item{reference}{Character scalar.  NCBI accession of the reference
genome (default \code{"NC_012920.1"} = rCRS) or a \link[Biostrings:DNAString-class]{Biostrings::DNAString}
/ \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} containing a single sequence.}

\item{genetic_codes}{Character vector of Biostrings genetic-code
identifiers to test.  Required (no default) — e.g. \code{c("SGC1", "SGC0")}
for the vertebrate mitochondrial and standard codes.}

\item{min_orf_length}{Integer. Minimum ORF length in nucleotides.
Default \code{60L}.}

\item{start_codons}{Character vector of recognised start codons.
Default \code{c("ATG","GTG","ATA","ATT","ATC")}.}

\item{substitution_tolerance}{Integer. Maximum number of amino-acid
differences allowed between a lost and gained ORF of equal length
for the pair to be classified as substitution-only (and excluded
from the disrupted/novel tally).  Default \code{3L}.}

\item{rna_structure}{Logical.  If \code{TRUE}, also fold the reference and
mutant RNA around the variant and report the secondary-structure
change.  Requires the ViennaRNA \code{RNAfold} binary (see \code{\link[=fold_rna]{fold_rna()}}).
Default \code{FALSE}.}

\item{rna_flank}{Integer.  Number of nucleotides included on each side of
the variant when folding (window length \verb{= 2 * rna_flank + 1}, clamped at
genome ends).  Default \code{100L}.}

\item{rna_strand}{Either \code{"sense"} (fold the reference strand as deposited)
or \code{"antisense"} (fold the reverse complement).  Default \code{"sense"}.}

\item{rna_temperature}{Numeric folding temperature in degrees Celsius,
passed to \code{\link[=fold_rna]{fold_rna()}}.  Default \code{37}.}

\item{rna_max_bulge}{Integer.  \code{max_bulge} passed to \code{\link[=longest_stem]{longest_stem()}} when
measuring the longest-stem change.  Default \code{2L}.}
}
\value{
A list with components:
\describe{
\item{\code{variant}}{Parsed variant (pos, ref, alt).}
\item{\code{reference}}{Reference accession or name used.}
\item{\code{summary}}{A \code{data.frame} with one row per genetic code,
columns \code{genetic_code}, \code{n_ref_orfs}, \code{n_alt_orfs}, \code{n_lost},
\code{n_gained}, \code{n_disrupted}, \code{n_novel}.}
\item{\code{detail}}{A named list (keyed by genetic code) of lists
containing \code{ref_orfs}, \code{alt_orfs}, \code{disrupted} (character
vector of protein sequences lost without substitution-only
counterpart), and \code{novel} (character vector of gained ORFs
without a substitution-only counterpart).}
\item{\code{rna}}{\code{NULL} unless \code{rna_structure = TRUE}, otherwise a list with
the folded window (\code{window}, \code{variant_offset}, \code{strand}), folding free
energies (\code{mfe_ref}, \code{mfe_alt}, \code{delta_mfe}), base-pair topology
(\code{n_pairs_ref}, \code{n_pairs_alt}, \code{shared_pairs}, \code{bp_distance},
\code{bp_distance_frac}), longest-stem change (\code{longest_stem_ref},
\code{longest_stem_alt}, \code{delta_stem}), and the two dot-bracket structures
(\code{structure_ref}, \code{structure_alt}).}
}
}
\description{
Introduces a single-base substitution into a reference mitogenome
(rCRS by default) and compares ORFs spanning the variant position
between reference and synthetic mutant under one or more genetic
codes.  Classifies each affected ORF as a true disruption, a novel
(read-through or new-start) ORF, or a substitution-only change.
}
\details{
Useful for assessing whether a longevity- or disease-associated
mtDNA SNP carries noncanonical coding consequences invisible to
standard variant-effect annotation.

The reference base at the specified position is verified against
the reference genome; mismatches raise an error.  For each genetic
code, \code{\link[=scan_orfs]{scan_orfs()}} is run on both the reference and a synthetic
single-mutant constructed by substituting the alt allele at the
variant position.  ORFs spanning the variant position are extracted
from each set; proteins unique to the reference are candidate
disruptions, proteins unique to the mutant are candidate novel
ORFs.  A lost/gained pair of equal protein length differing by no
more than \code{substitution_tolerance} residues is classified as
substitution-only and excluded from both tallies.

When \code{rna_structure = TRUE}, a window of \code{rna_flank} nt either side of the
variant is folded on the chosen strand for both reference and mutant via
\code{\link[=fold_rna]{fold_rna()}} (which requires the ViennaRNA \code{RNAfold} binary on \code{PATH}), and
the change is quantified as the shift in folding free energy (\code{delta_mfe}),
the base-pair distance (pairs gained + lost between the two structures), and
the change in the longest stem.  The window is clamped at the linear genome
ends — circularity is not wrapped, so variants within \code{rna_flank} of the
deposit boundary get a truncated structural context.
}
\examples{
\dontrun{
# The classic D4a longevity variant (Japanese centenarians)
res <- analyze_snp("m.3206C>T", genetic_codes = c("SGC1", "SGC0"))
res$summary

# m.T16189C — extends a minus-strand D-loop peptide under mt code
res16189 <- analyze_snp("m.16189T>C", genetic_codes = c("SGC1", "SGC0"))
res16189$detail[["SGC1"]]$novel

# RNA structural impact of m.3206C>T (Liu et al. 2007 reported minimal effect)
res_rna <- analyze_snp("m.3206C>T", genetic_codes = c("SGC1", "SGC0"),
                       rna_structure = TRUE)
res_rna$rna$delta_mfe      # small shift in folding free energy
res_rna$rna$bp_distance    # few base pairs gained / lost
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=compare_mitogenomes]{compare_mitogenomes()}}, \code{\link[=fold_rna]{fold_rna()}},
\code{\link[=pair_partners]{pair_partners()}}, \code{\link[=longest_stem]{longest_stem()}}
}
