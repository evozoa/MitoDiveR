% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_regions.R
\name{annotate_genomic_regions}
\alias{annotate_genomic_regions}
\title{Annotate ORFs with Their Mitogenomic Region}
\usage{
annotate_genomic_regions(orfs, features, seq_len = NULL)
}
\arguments{
\item{orfs}{A \code{data.frame} returned by \code{\link[=find_orfs]{find_orfs()}}.  Must contain
\code{start} and \code{strand} columns.}

\item{features}{A \code{data.frame} of GenBank features as returned in the
\verb{$features} element of \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}.  Must contain \code{type},
\code{start}, \code{end}, \code{strand}, \code{gene}, and \code{product} columns.}

\item{seq_len}{Integer.  Total length of the genome in nucleotides.
Required only to handle ORFs that wrap around the origin
(\code{wraps_around == TRUE}); ignored otherwise.  Defaults to \code{NULL}
(wrap-around ORFs are annotated by their \code{start} coordinate only).}
}
\value{
The input \code{orfs} data.frame with a \code{genomic_region} character
column appended.
}
\description{
Adds a \code{genomic_region} column to an ORF table (output of \code{\link[=find_orfs]{find_orfs()}})
by mapping each ORF's start-codon position against a GenBank feature table
(output of \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}).
}
\details{
Region labels follow this priority (highest wins when features overlap):
\enumerate{
\item \verb{CDS:<gene>} — protein-coding gene
\item \verb{tRNA:<product>} — transfer RNA
\item \verb{<gene>} for rRNA features (e.g. \verb{12S_rRNA}, \verb{16S_rRNA})
\item \code{D-loop} — control / displacement loop
\item Name of any other annotated feature
\item \code{intergenic} — falls outside all annotated features
}
}
\examples{
\dontrun{
genomes <- fetch_mito_genbank(c("NC_002333.2", "NC_028087.1"))

orfs_danio <- find_orfs(genomes[["NC_002333.2"]]$sequence)
orfs_danio$seq_id <- "NC_002333.2"
orfs_danio <- annotate_genomic_regions(
  orfs_danio,
  genomes[["NC_002333.2"]]$features,
  seq_len = genomes[["NC_002333.2"]]$length
)
table(orfs_danio$genomic_region)
}

}
