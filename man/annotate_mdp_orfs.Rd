% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_mdp_orfs.R
\name{annotate_mdp_orfs}
\alias{annotate_mdp_orfs}
\title{Label ORFs With the Names of Known Mitochondrial-Derived Peptides}
\usage{
annotate_mdp_orfs(
  orfs,
  reference = .stop_no_mdp_reference(),
  method = c("exact", "homology"),
  min_identity = 60,
  min_coverage = 60,
  prefix = "mdp"
)
}
\arguments{
\item{orfs}{\code{data.frame} of ORFs -- typically from \code{\link[=scan_orfs]{scan_orfs()}},
\code{\link[=find_orfs]{find_orfs()}} or \code{\link[=collapse_nested_orfs]{collapse_nested_orfs()}}.  Must contain a
\code{protein_sequence} column.  A trailing stop-codon \code{*} is ignored.}

\item{reference}{Peptide sequences to match against.  A named \code{character}
vector, an \link[Biostrings:XStringSet-class]{Biostrings::AAStringSet}, or a path to a FASTA file.  Names are
used as MDP identifiers.  \strong{Required -- there is no default}, because which
peptide set a screen was run against belongs in the record.  \code{\link[=mdp_sequences]{mdp_sequences()}}
supplies the human set (Swiss-Prot sequences, each verified to be encoded in
the rCRS under the standard genetic code); pass it explicitly to use it.}

\item{method}{Either \code{"exact"} (default) or \code{"homology"}.  \code{"exact"} matches
by string identity, including the containment cases where the ORF extends
past the annotated peptide or vice versa.  \code{"homology"} runs
\code{\link[=score_mdp_similarity]{score_mdp_similarity()}} and keeps hits passing \code{min_identity} and
\code{min_coverage}.}

\item{min_identity, min_coverage}{Numeric (0--100).  Thresholds applied when
\code{method = "homology"}; ignored for \code{"exact"}.  Defaults \code{60} and \code{60}.
The permissive defaults of \code{\link[=score_mdp_similarity]{score_mdp_similarity()}} are deliberately \emph{not}
inherited: at 20\% identity, unrelated peptides of MDP length routinely
score as hits.}

\item{prefix}{Character scalar prepended to the names of the added columns.
Default \code{"mdp"}.}
}
\value{
\code{orfs} with four columns added (named using \code{prefix}):
\describe{
\item{\code{mdp_name}}{Name of the matched reference peptide, or \code{NA} when the
ORF matched nothing.}
\item{\code{mdp_match}}{How it matched: \code{"identical"}, \code{"orf_contains_mdp"},
\code{"mdp_contains_orf"}, \code{"homology"}, or \code{NA}.}
\item{\code{mdp_pct_identity}}{Percent identity.  \code{100} for \code{"identical"} and
for the containment cases (the shared region is exact); the alignment
value for \code{"homology"}; \code{NA} for no match.}
\item{\code{mdp_coverage}}{Percentage of the reference peptide accounted for by
the ORF.  \code{NA} for no match.}
}
Row order and all existing columns are preserved.
}
\description{
Attaches the identity of a known mitochondrial-derived peptide (MDP) to each
row of an ORF table, so that downstream summaries and figures -- notably
\code{\link[=plot_rna_structure]{plot_rna_structure()}} -- can name the ORFs they draw instead of showing them
as anonymous spans.
}
\details{
Two matching methods are offered because the two situations they serve are
genuinely different.  Within the species the reference peptides came from,
an ORF either \emph{is} a given MDP or it is not, and \code{method = "exact"} settles
that definitionally with no thresholds to tune.  Across species the peptides
have diverged and only a scored alignment can decide, which is what
\code{method = "homology"} provides.

When an ORF matches more than one reference peptide, the longest matching
reference wins under \code{"exact"} and the highest alignment score wins under
\code{"homology"}.  A single reference peptide may legitimately be assigned to
several ORFs -- overlapping and nested ORFs are common in rRNA genes -- so no
one-to-one constraint is imposed.

Matching is on amino acid sequence alone and takes no account of coordinates,
so the caller remains responsible for having scanned the right molecule with
the right genetic code.  Human MDPs are translated on cytoplasmic 80S
ribosomes and therefore require the standard code (\code{"SGC0"}), not the
vertebrate mitochondrial code.
}
\examples{
\dontrun{
mdps <- c(Humanin = "MAPRGFSCLLLLTSEIDLPVKRRA",
          SHLP6   = "MLDQDIPMVQPLLKVRLFND")

orfs <- scan_orfs(rnr2, min_orf_length = 60, genetic_code = "SGC0")
orfs <- annotate_mdp_orfs(orfs, reference = mdps)
orfs[!is.na(orfs$mdp_name), c("start", "end", "strand", "mdp_name")]

# Named ORFs are drawn and labelled; the rest stay as context
plot_rna_structure(fold, orfs = orfs, offset = 1671)

# Across species, where the peptides have diverged
annotate_mdp_orfs(killifish_orfs, reference = mdps, method = "homology",
                  min_identity = 50)
}

}
\seealso{
\code{\link[=plot_rna_structure]{plot_rna_structure()}}, \code{\link[=score_mdp_similarity]{score_mdp_similarity()}}, \code{\link[=scan_orfs]{scan_orfs()}}
}
