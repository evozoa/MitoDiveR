% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_reference_frame.R
\name{annotate_reference_frame}
\alias{annotate_reference_frame}
\title{Anchor ORF Reading Frames to a Shared Reference Coordinate System}
\usage{
annotate_reference_frame(
  orfs,
  genomes,
  anchor = NULL,
  gap_opening = 5,
  gap_extension = 2,
  prefix = "ref",
  verbose = TRUE
)
}
\arguments{
\item{orfs}{\code{data.frame} from \code{\link[=scan_orfs]{scan_orfs()}} / \code{\link[=find_orfs]{find_orfs()}} /
\code{\link[=collapse_nested_orfs]{collapse_nested_orfs()}}. Must contain \code{seq_id}, \code{strand}, \code{start}, and
\code{end} columns. ORFs from more than one genetic-code scan of the same
genomes can be combined (e.g. \code{rbind()}ed) before calling — the alignment
step is independent of genetic code and translation, so it only needs to
run once either way.}

\item{genomes}{Named \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} or named character vector
of the genome sequences \code{orfs$seq_id} refers to — the same object passed
to \code{\link[=scan_orfs]{scan_orfs()}}. Must include every value in \code{orfs$seq_id}, including
\code{anchor}.}

\item{anchor}{Character scalar naming the sequence in \code{genomes} to use as
the shared coordinate system. Its choice is arbitrary — the anchor is a
ruler, not a biological reference — but it must be the same anchor for
every call being compared. Defaults to \code{names(genomes)[1]}, with a
message identifying the sequence used.}

\item{gap_opening, gap_extension}{Numeric. Gap penalties passed to
\code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}} for the nucleotide alignment of each
non-anchor genome against \code{anchor}. Defaults \code{5} and \code{2}.}

\item{prefix}{Character scalar prepended to the names of the added columns.
Default \code{"ref"}.}

\item{verbose}{Logical. Report progress as each genome is aligned to the
anchor? Default \code{TRUE}.}
}
\value{
\code{orfs} with four columns added (named using \code{prefix}):
\describe{
\item{\code{ref_5p}}{Anchor-coordinate position of the ORF's start codon (the
\code{start} column on the \code{+} strand, \code{end} on the \code{-} strand, per
\code{\link[=collapse_nested_orfs]{collapse_nested_orfs()}}'s stop/start convention).}
\item{\code{ref_start}, \code{ref_end}}{Anchor-coordinate span of the ORF
(\code{start}/\code{end} mapped through the alignment; always \code{ref_start <= ref_end}).}
\item{\code{ref_frame}}{\code{"<strand>_<(ref_5p - 1) \%\% 3>"}, e.g. \code{"-_1"}. Two
ORFs sharing this value are in the same anchor-relative frame.}
}
All four are \code{NA} for an ORF whose start codon aligns before the anchor's
first base (no anchor coordinate exists there). Row order and all existing
columns are preserved.
}
\description{
Reading \code{frame} in \code{\link[=find_orfs]{find_orfs()}} / \code{\link[=scan_orfs]{scan_orfs()}} output is \verb{start position mod 3} in each genome's own coordinates, so it drifts across genomes with
every indel accumulated since their common ancestor — two truly orthologous
ORFs routinely end up with different frame numbers, and two unrelated
overlapping ORFs can just as easily share one. Frame numbers are therefore
not comparable across sequences on their own.
}
\details{
This function fixes that by aligning every sequence to a single shared
anchor sequence once, mapping each ORF's start-codon (5') position through
that alignment, and labelling it with \verb{anchor_position mod 3} (combined
with strand). Two ORFs from different genomes with the same
\verb{<prefix>_frame} value sit in the same phase of the same anchor-relative
reading frame — a cheap, alignment-verified fact that a raw coordinate
overlap cannot confirm on its own. It is a filter for candidate positional
orthologs, not a homology call: overlapping, same-frame ORFs still need an
amino-acid comparison (e.g. \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, or a direct
\code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}} on \code{protein_sequence}) to confirm they are
actually the same peptide, since two unrelated ORFs can coincidentally
share a frame.

One global nucleotide alignment is computed per non-anchor sequence in
\code{genomes} (the anchor maps to itself with no alignment needed), not one per
ORF or per pair of ORFs — cost scales with the number of genomes, not the
number of ORFs or ORF pairs. For \code{n} genomes that is \code{n - 1} alignments
regardless of how many times "same frame?" is subsequently asked, which is
the point: it replaces a pairwise, quadratic-in-genomes alignment burden
with a linear one.
}
\examples{
\dontrun{
orfs <- scan_orfs(seqs, genetic_code = "SGC0", min_orf_length = 60,
                   max_orf_length = 900, start_codons = "ATG")
orfs <- collapse_nested_orfs(orfs)
orfs <- annotate_reference_frame(orfs, genomes = seqs,
                                  anchor = names(seqs)[1])

# candidate positional orthologs: same strand, same anchor frame, overlapping
same_frame <- split(orfs, orfs$ref_frame)
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=find_orfs]{find_orfs()}}, \code{\link[=collapse_nested_orfs]{collapse_nested_orfs()}},
\code{\link[=find_conserved_orfs]{find_conserved_orfs()}}
}
