% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blast_orfs.R
\name{blast_orfs}
\alias{blast_orfs}
\title{BLAST ORF Protein Sequences Against NCBI}
\usage{
blast_orfs(
  orfs,
  database = "nr",
  max_hits = 10L,
  evalue_threshold = 0.01,
  poll_interval = 15L,
  timeout = 300L
)
}
\arguments{
\item{orfs}{\code{data.frame} from \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} or \code{\link[=scan_orfs]{scan_orfs()}}.
Must contain a \code{protein_sequence} column.  If a \code{cluster_id} column is
present, one representative per cluster is submitted.}

\item{database}{Character scalar.  NCBI BLAST database.  Default \code{"nr"}.
Use \code{"swissprot"} for faster, curated hits.}

\item{max_hits}{Integer.  Maximum number of hits to return per query.
Default \code{10L}.}

\item{evalue_threshold}{Numeric.  Maximum E-value to report.  Default \code{0.01}.}

\item{poll_interval}{Integer.  Seconds between status polls.  Default \code{15L}.}

\item{timeout}{Integer.  Maximum seconds to wait for results.  Default \code{300L}.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{\code{query_id}}{Sequence identifier submitted (cluster_id + seq_id, or
row index).}
\item{\code{cluster_id}}{Cluster identifier, if present in input.}
\item{\code{hit_accession}}{NCBI accession of the hit.}
\item{\code{hit_description}}{Hit sequence description.}
\item{\code{evalue}}{Alignment E-value.}
\item{\code{pct_identity}}{Percent identity.}
\item{\code{query_coverage}}{Fraction of query covered by the alignment.}
\item{\code{hit_length}}{Length of the hit sequence.}
}
}
\description{
Submits protein sequences from a \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} (or \code{\link[=scan_orfs]{scan_orfs()}})
result to NCBI BLASTP and returns a tidy table of hits.  By default one
representative sequence per cluster is submitted (the longest), keeping
API usage minimal.
}
\details{
Uses the NCBI BLAST URL API.  An internet connection is required.  NCBI
requests that automated submissions include a contact email — set one with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(MitoDiveR.email = "you@example.com")
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, \code{\link[=scan_orfs]{scan_orfs()}}
}
