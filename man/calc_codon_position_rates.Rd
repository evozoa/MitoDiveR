% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_codon_position_rates.R
\name{calc_codon_position_rates}
\alias{calc_codon_position_rates}
\title{Calculate Per-Codon-Position Substitution Rates}
\usage{
calc_codon_position_rates(alignment, cluster_id = NULL, return_pairs = FALSE)
}
\arguments{
\item{alignment}{One of:
\itemize{
\item A \code{character} vector of equal-length, gap-free, in-frame DNA
strings (length must be a multiple of 3).
\item A path to a FASTA file containing a codon-aligned set of sequences.
\item A \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} of equal-length sequences.
\item A \code{data.frame} from \code{\link[=cluster_conserved_orfs]{cluster_conserved_orfs()}} with an
\code{orf_sequence} column; if a \code{cluster_id} column is present and
more than one cluster is represented, \code{cluster_id} must also be
supplied.
}}

\item{cluster_id}{Optional cluster identifier when \code{alignment} is a
multi-cluster \code{\link[=cluster_conserved_orfs]{cluster_conserved_orfs()}} result.}

\item{return_pairs}{Logical.  If \code{TRUE}, the per-pair matrix of
position-wise proportions is returned in \verb{$pairs}.  Default \code{FALSE}.}
}
\value{
A list with:
\describe{
\item{\code{by_position}}{\code{data.frame} with one row per codon position
(1, 2, 3) and columns \code{n_sites}, \code{n_subs}, \code{prop_diff}, and
\code{jc_dist} (Jukes-Cantor corrected; \code{NA} if \code{prop_diff} >= 0.75).}
\item{\code{ratios}}{Named numeric vector of position-rate ratios
(\code{p3_over_p1}, \code{p3_over_p2}, \code{p3_over_mean_p1p2}).}
\item{\code{chisq}}{\code{htest} object from \code{\link[=chisq.test]{chisq.test()}} on the 3 x 2
contingency table of substitutions versus invariant sites.}
\item{\code{n_sequences}, \code{n_codons}, \code{n_pairs}}{Integer scalars
describing the alignment.}
\item{\code{pairs}}{(optional) \code{n_pairs} x 3 matrix of pairwise
proportions of differences at each codon position; columns
\code{p1}, \code{p2}, \code{p3}.  Returned only when \code{return_pairs = TRUE}.}
}
}
\description{
For an in-frame codon alignment, computes the proportion of nucleotide
differences at first, second, and third codon positions across all
pairwise sequence comparisons.  Tests the null hypothesis of equal
substitution rates across the three codon positions with a 3 x 2
chi-square contingency test on substitutions versus invariant sites.
}
\details{
Canonical protein-coding genes show a strongly skewed signature with
the third position evolving fastest (most synonymous), the first
position intermediate, and the second position most constrained.
Departures from this pattern may indicate weakened protein-level
constraint, overlapping selection from the antisense strand (as in
rRNA-embedded MDP loci), or non-coding origin of the reading frame.

Gaps and ambiguous bases (anything outside \verb{\{A, C, G, T\}}) are treated
as missing and excluded from both numerator and denominator at the
affected site within each pair.  The Jukes-Cantor correction is
\deqn{d = -\tfrac{3}{4}\ln\!\left(1 - \tfrac{4}{3} p\right)}
and returns \code{NA} for \code{p >= 0.75} (saturation).
}
\examples{
\dontrun{
# From a codon-aligned FASTA
res <- calc_codon_position_rates("shlp3_codon_aln.fas")
res$by_position
res$chisq

# From a cluster_conserved_orfs() result
clusters <- cluster_conserved_orfs(orfs)
calc_codon_position_rates(clusters, cluster_id = "cl9")
}

}
\seealso{
\code{\link[=calc_dnds]{calc_dnds()}}, \code{\link[=cluster_conserved_orfs]{cluster_conserved_orfs()}}
}
