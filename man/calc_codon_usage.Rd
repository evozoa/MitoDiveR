% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_codon_usage.R
\name{calc_codon_usage}
\alias{calc_codon_usage}
\title{Calculate Codon Usage Bias for ORF Sets}
\usage{
calc_codon_usage(orfs, genetic_code = "SGC1", per_sequence = TRUE)
}
\arguments{
\item{orfs}{\code{data.frame} from \code{\link[=scan_orfs]{scan_orfs()}} or \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}.
Must contain an \code{orf_sequence} column.}

\item{genetic_code}{Character scalar.  Biostrings genetic-code identifier
passed to \code{\link[Biostrings:GENETIC_CODE]{Biostrings::getGeneticCode()}}.  Default \code{"SGC1"} (vertebrate
mitochondrial code).  Must match the code used when scanning ORFs.}

\item{per_sequence}{Logical.  If \code{TRUE} (default) and a \code{seq_id} column is
present, RSCU is computed separately for each source sequence.  If
\code{FALSE}, all ORFs are pooled before counting.}
}
\value{
A \code{data.frame} with one row per codon (per sequence when
\code{per_sequence = TRUE}), sorted by \code{seq_id} then \code{amino_acid} then
\code{codon}:
\describe{
\item{\code{seq_id}}{Source sequence identifier (only when
\code{per_sequence = TRUE} and \code{seq_id} is present in input).}
\item{\code{codon}}{Three-letter nucleotide codon.}
\item{\code{amino_acid}}{Single-letter amino acid encoded by this codon under
the selected genetic code.}
\item{\code{n_synonymous_codons}}{Number of codons in the synonymous family
(including this codon).}
\item{\code{count}}{Observed count of this codon across all ORFs in the
group.}
\item{\code{rscu}}{Relative synonymous codon usage.  Computed as observed
count divided by expected count under uniform synonymous usage:
\code{RSCU = count / (family_total / n_synonymous_codons)}.  \code{NA} when
the entire synonymous family has zero counts.}
}
}
\description{
Computes Relative Synonymous Codon Usage (RSCU) for the coding sequences
returned by \code{\link[=scan_orfs]{scan_orfs()}} or \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}.  RSCU normalises
observed codon counts within each synonymous family so that uniform usage
gives 1.0; values above 1.0 indicate over-representation and below 1.0
indicate under-representation.  Stop codons are excluded from all
calculations.
}
\details{
\subsection{RSCU interpretation}{

An RSCU of 1.0 means a codon is used exactly as often as expected if all
synonymous codons were equally preferred.  Values markedly different from
1.0 indicate translational selection or mutational bias.  Because RSCU is
computed within synonymous families, it is not affected by amino-acid
composition.
}

\subsection{Genetic code}{

The genetic code affects which codons are synonymous.  In the vertebrate
mitochondrial code (\code{"SGC1"}), AGA and AGG are stop codons (not Arg), and
ATA encodes Met (not Ile), collapsing some synonymous families relative to
the standard code.  Always use the code that matches the analysed genome.
}
}
\examples{
\dontrun{
seqs <- fetch_sequences(c("NC_012920.1", "NC_011137.1"))
orfs <- scan_orfs(seqs, min_orf_length = 60)

# Per-sequence RSCU (mito code)
rscu <- calc_codon_usage(orfs)

# Pooled across all sequences, standard code
rscu_std <- calc_codon_usage(orfs, genetic_code = "SGC0",
                              per_sequence = FALSE)

# Highly biased codons (RSCU > 1.5 or < 0.5)
rscu[!is.na(rscu$rscu) & (rscu$rscu > 1.5 | rscu$rscu < 0.5), ]
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, \code{\link[=calc_dnds]{calc_dnds()}}
}
