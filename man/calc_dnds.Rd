% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_dnds.R
\name{calc_dnds}
\alias{calc_dnds}
\title{Calculate pairwise dN/dS for Conserved ORF Clusters}
\usage{
calc_dnds(orfs, genetic_code = .stop_no_code("genetic_code"))
}
\arguments{
\item{orfs}{\code{data.frame} from \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}.  Must contain
\code{cluster_id}, \code{seq_id}, \code{orf_sequence}, and \code{protein_sequence} columns.}

\item{genetic_code}{Character scalar.  Biostrings genetic-code identifier.
Required (no default).  Must match the code
used when scanning ORFs.}
}
\value{
A \code{data.frame} with one row per sequence pair per cluster:
\describe{
\item{\code{cluster_id}}{Cluster identifier from \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}.}
\item{\code{seq_id_1}, \code{seq_id_2}}{Identifiers of the two sequences
compared.}
\item{\code{n_codons}}{Number of codon positions in the alignment (gap-free
columns only).}
\item{\code{S}}{Total synonymous sites (averaged across both sequences).}
\item{\code{N}}{Total nonsynonymous sites.}
\item{\code{Sd}}{Observed synonymous differences.}
\item{\code{Nd}}{Observed nonsynonymous differences.}
\item{\code{pS}}{Proportion of synonymous differences (\code{Sd / S}).}
\item{\code{pN}}{Proportion of nonsynonymous differences (\code{Nd / N}).}
\item{\code{dS}}{Synonymous distance after Jukes-Cantor correction.  \code{NA}
when \code{pS >= 0.75} (saturation).}
\item{\code{dN}}{Nonsynonymous distance after Jukes-Cantor correction.  \code{NA}
when \code{pN >= 0.75}.}
\item{\code{omega}}{dN/dS ratio.  \code{NA} when either distance is \code{NA}.
\code{Inf} when \code{dS = 0} and \code{dN > 0} (no synonymous change detected).
Values < 1 indicate purifying selection; ~1 neutral evolution;
> 1 positive selection.}
}
Returns an empty \code{data.frame} invisibly (with a message) if no valid codon
alignments are found.
}
\description{
Estimates the ratio of nonsynonymous to synonymous substitution rates
(dN/dS, also written Ka/Ks or ω) for each orthologous ORF cluster
identified by \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}.  Protein sequences are globally
aligned to guide a codon-level alignment, and substitution counts are
computed using the Nei-Gojobori (1986) method with Jukes-Cantor distance
correction.
}
\details{
\subsection{Method}{

The Nei-Gojobori (1986) method counts synonymous and nonsynonymous sites
per codon by considering all single-nucleotide changes at each codon
position.  For codon pairs differing at two or three positions, observed
differences are averaged over all possible mutational pathways, excluding
paths that pass through stop codons.  The raw proportions pS and pN are
corrected with the Jukes-Cantor formula:
\deqn{d = -\tfrac{3}{4}\ln\!\left(1 - \tfrac{4}{3}\,p\right)}
Codons containing ambiguous bases (non-ACGT) and aligned stop codons are
skipped.
}

\subsection{Interpretation caveats}{

dN/dS is estimated per cluster from as few as two sequences, so individual
values can be noisy.  Very short ORFs (< 100 nt) yield wide confidence
intervals.  A saturated synonymous distance (\code{pS >= 0.75}) returns \code{NA}
for dS and omega; this is common for highly diverged sequences or
ORFs in fast-evolving regions.
}
}
\examples{
\dontrun{
seqs      <- fetch_sequences(c("NC_012920.1", "NC_011137.1"))
orfs      <- scan_orfs(seqs, min_orf_length = 60)
conserved <- find_conserved_orfs(orfs, min_identity = 50)
dnds      <- calc_dnds(conserved)

# Clusters under putative positive selection
dnds[!is.na(dnds$omega) & dnds$omega > 1, ]

# Clusters with strong purifying selection
dnds[!is.na(dnds$omega) & dnds$omega < 0.3, ]
}

}
\references{
Nei M, Gojobori T (1986) Simple methods for estimating the numbers of
synonymous and nonsynonymous nucleotide substitutions.
\emph{Molecular Biology and Evolution} \strong{3}(5):418–426.
}
\seealso{
\code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, \code{\link[=calc_codon_usage]{calc_codon_usage()}}
}
