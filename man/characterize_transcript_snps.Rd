% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/characterize_transcript_snps.R
\name{characterize_transcript_snps}
\alias{characterize_transcript_snps}
\title{Characterize SNPs within a noncanonical mtRNA transcript (transcript-first)}
\usage{
characterize_transcript_snps(
  region,
  taxon = "Homo sapiens",
  reference = "NC_012920.1",
  source = c("auto", "mitomap", "ncbi"),
  min_freq = 0,
  max_genomes = 1000L,
  genetic_codes = .stop_no_code("genetic_codes"),
  min_orf_length = 45L,
  analysis_flank = 300L,
  rna_structure = TRUE,
  mitomap_file = NULL,
  ...
)
}
\arguments{
\item{region}{Either a preset transcript name (\code{"MOTS-c"}, \code{"humanin"}) or a
list with \code{start}, \code{end}, and optional \code{strand} (\code{"+"}/\code{"-"}) giving the
region's coordinates on \code{reference}.}

\item{taxon}{NCBI organism name.  Default \code{"Homo sapiens"}.}

\item{reference}{Reference genome: NCBI accession (default \code{"NC_012920.1"} =
rCRS) or a \link[Biostrings:DNAString-class]{Biostrings::DNAString}.}

\item{source}{One of \code{"auto"} (MITOMAP for \emph{Homo sapiens}, NCBI otherwise),
\code{"mitomap"}, or \code{"ncbi"}.}

\item{min_freq}{Numeric.  Only characterize variants at or above this
population/sample frequency, expressed as a fraction in \verb{[0, 1]} (MITOMAP
percentages are converted to fractions internally).  Default \code{0} (all).}

\item{max_genomes}{Integer.  Cap on NCBI mitogenomes sampled (non-human
path).  Default \code{1000L}.  Set \code{Inf} for the full set (warns if very large).}

\item{genetic_codes, min_orf_length, rna_structure}{Passed to \code{\link[=analyze_snp]{analyze_snp()}}.
\code{min_orf_length} defaults to \code{45L} here so short MDP ORFs (MOTS-c, SHLPs)
are detected.}

\item{analysis_flank}{Integer.  Each variant is characterized against a
window of the reference spanning the transcript +/- this many nt rather
than the whole genome (the key speedup — full-genome rescans per variant
are prohibitively slow).  Keep it larger than the longest ORF of interest
and >= \code{analyze_snp()}'s \code{rna_flank}.  Default \code{300L}.}

\item{mitomap_file}{Optional path to a previously downloaded MITOMAP
polymorphisms TSV; if \code{NULL} it is downloaded once and cached in the
session temp directory.}

\item{...}{Further arguments passed to \code{\link[=analyze_snp]{analyze_snp()}}.}
}
\value{
A list with \code{transcript} (resolved region), \code{source}, \code{n_genomes}
(NCBI path only), and \code{variants} — a \code{data.frame} with one row per
characterized SNP: \code{pos}, \code{ref}, \code{alt}, \code{freq}, \code{count}, \code{consequence}
(\code{"disrupts_orf"} / \code{"novel_orf"} / \code{"substitution"} / \code{"no_orf"}),
\code{n_disrupted}, \code{n_novel}, and (if \code{rna_structure}) \code{delta_mfe},
\code{bp_distance}, \code{delta_stem}.  Variants that error in \code{\link[=analyze_snp]{analyze_snp()}} (e.g.
reference-base mismatch) are reported in the \code{failed} attribute.
}
\description{
The transcript-first complement to \code{\link[=analyze_snp]{analyze_snp()}}.  Rather than starting
from a single variant, you supply a transcript region; the function
enumerates the single-nucleotide variants observed within it for a taxon and
runs each through \code{\link[=analyze_snp]{analyze_snp()}} to report peptide and (optionally) RNA
secondary-structure consequences alongside the variant's population
frequency.
}
\details{
Variant sources by taxon:
\itemize{
\item \strong{Human} (\code{taxon = "Homo sapiens"}): the MITOMAP GenBank
polymorphisms table (pre-computed allele frequencies from ~60k+ GenBank
mitogenomes), in rCRS coordinates.  No genome download.
\item \strong{Other taxa}: a sample of up to \code{max_genomes} mitogenomes is
fetched from NCBI, the region is extracted, and substitutions relative to
\code{reference} are tabulated with their sample frequency.
}

MITOMAP coordinates and \code{ref} alleles are rCRS; if you supply a non-rCRS
\code{reference} the human path will mismatch.  The NCBI path fetches each record
by coordinate (\code{seq_start}/\code{seq_stop}) and currently skips records whose
region length differs from the reference (indels) — adequate for
within-species surveys where indels are rare in coding/rRNA regions.

Preset coordinates are rCRS-based and \strong{provisional} — verify against your
annotation before publication.  ASncmtRNA-1/-2 are intentionally not
presets (their transcript boundaries vary in the literature); supply them as
explicit \code{list(start, end, strand = "-")}.
}
\examples{
\dontrun{
# Human MOTS-c region — should surface the K14Q variant (m.1382A>C)
res <- characterize_transcript_snps("MOTS-c", min_freq = 0.001)
res$variants

# An explicit antisense transcript in another species
characterize_transcript_snps(
  list(start = 1671, end = 3229, strand = "-"),
  taxon = "Sebastes", reference = "NC_005450.1", max_genomes = 50)
}

}
\seealso{
\code{\link[=analyze_snp]{analyze_snp()}}, \code{\link[=fold_rna]{fold_rna()}}, \code{\link[=fetch_sequences]{fetch_sequences()}}
}
