% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse_nested_orfs.R
\name{collapse_nested_orfs}
\alias{collapse_nested_orfs}
\title{Collapse nested ORFs to the longest ORF per stop codon}
\usage{
collapse_nested_orfs(orfs, length_col = "length_nt")
}
\arguments{
\item{orfs}{A \code{data.frame} from \code{\link[=scan_orfs]{scan_orfs()}} / \code{\link[=find_orfs]{find_orfs()}}, requiring
\code{frame}, \code{strand}, \code{start}, \code{end} columns.}

\item{length_col}{Name of the column to maximise when choosing the
representative ORF per stop.  Default \code{"length_nt"}; if absent it is
derived as \code{end - start + 1}.}
}
\value{
The input \code{data.frame} with nested ORFs removed — one row per
\verb{(seq_id, frame, stop)} — preserving all original columns and row order by
genomic \code{start}.  Returned unchanged (with a warning) if the required
columns are absent.
}
\description{
\code{\link[=scan_orfs]{scan_orfs()}} / \code{\link[=find_orfs]{find_orfs()}} report \emph{every} start-codon-to-stop ORF, so a
single stop-free region yields many nested ORFs (one per internal start
codon, all sharing the same stop).  This collapses each such family to the
single longest ORF terminating at a given stop, within each reading frame —
i.e. the distinct ORF \emph{loci} rather than every nested start.  Use it to get
meaningful ORF counts and uncluttered plots (e.g. \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}}).
}
\details{
The stop codon's genomic coordinate is the \code{end} on the \code{+} strand and the
\code{start} on the \code{-} strand (the table keeps \code{start <= end}, so on the minus
strand the stop sits in the \code{start} column).  ORFs are grouped by
\verb{(seq_id, strand, frame, stop coordinate)} and the longest member of each
group is kept.  Grouping includes \code{seq_id} when present, so ORFs sharing
coordinates across \emph{different} sequences are kept as the distinct loci they
are rather than collapsed together.
}
\examples{
\dontrun{
orfs <- scan_orfs(seqs, genetic_code = "SGC1", min_orf_length = 150)
nrow(orfs)                       # many, incl. nested starts
loci <- collapse_nested_orfs(orfs)
nrow(loci)                       # one per (frame, stop)
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=find_orfs]{find_orfs()}}, \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}}
}
