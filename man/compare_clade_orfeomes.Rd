% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_clade_orfeomes.R
\name{compare_clade_orfeomes}
\alias{compare_clade_orfeomes}
\title{Compare ORFeomes across a clade (taxon-first)}
\usage{
compare_clade_orfeomes(
  taxon,
  genetic_code = .stop_no_code("genetic_code"),
  max_genomes = 20L,
  refseq_only = TRUE,
  min_length = 13000L,
  min_orf_length = 60L,
  start_codons = c("ATG", "GTG", "ATA", "ATT", "ATC"),
  noncanonical_only = TRUE,
  collapse_nested = TRUE,
  min_identity = 70,
  gap_handling = c("pairwise_complete", "mismatch"),
  also_jaccard = TRUE,
  mafft_bin = "mafft",
  mafft_opts = "--auto",
  api_key = NULL
)
}
\arguments{
\item{taxon}{NCBI organism / taxon name (e.g. \code{"Hominidae"}).}

\item{genetic_code}{Required (no default) — Biostrings genetic-code id, e.g.
\code{"SGC1"} (vertebrate mitochondrial) or \code{"SGC0"} (standard).}

\item{max_genomes}{Integer cap on mitogenomes fetched.  Default \code{20L}.}

\item{refseq_only}{Restrict the NCBI search to RefSeq?  Default \code{TRUE}
(one representative per species; balances species representation).}

\item{min_length}{Integer.  Minimum genome length (nt) to retain.
Default \code{13000L}.}

\item{min_orf_length}{Integer.  Passed to \code{\link[=scan_orfs]{scan_orfs()}}.  Default \code{60L}
(catches MDP-scale ORFs).}

\item{start_codons}{Character vector of recognised start codons passed to
\code{\link[=scan_orfs]{scan_orfs()}}.}

\item{noncanonical_only}{Logical.  If \code{TRUE} (default), drop ORFs whose 5'
position coincides with the 5' end of a CDS feature on the same strand
(i.e. the canonical 13 mt CDSs).  All other ORFs — minus-strand,
frame-shifted same-strand, rRNA/D-loop-internal — are retained.}

\item{collapse_nested}{Logical.  If \code{TRUE} (default), call
\code{\link[=collapse_nested_orfs]{collapse_nested_orfs()}} before cross-genome clustering so each (frame,
stop) per genome contributes a single longest ORF rather than every
nested internal start codon.  Cuts the clustering input from ~10³ to ~10²
ORFs per genome and is essential for tractable runtime; turn off only if
you need every nested start.}

\item{min_identity}{Numeric percent aa identity passed to
\code{\link[=find_conserved_orfs]{find_conserved_orfs()}} for cross-genome family clustering.  Default \code{70}.}

\item{gap_handling}{How to treat gap-vs-residue positions in pairwise
identity: \code{"pairwise_complete"} (default; ignore positions where either
sequence has a gap — measures sequence divergence within shared ORF
regions) or \code{"mismatch"} (count gaps as mismatches — folds in
lineage-specific gain/loss).}

\item{also_jaccard}{Logical.  Also compute a genome × genome Jaccard
distance matrix on family presence/absence?  Default \code{TRUE}.}

\item{mafft_bin}{Path to the MAFFT binary.  Default \code{"mafft"} (must be on
\code{PATH}).}

\item{mafft_opts}{Character options passed to MAFFT.  Default \code{"--auto"}.}

\item{api_key}{Optional NCBI API key.}
}
\value{
A list with components:
\describe{
\item{\code{taxon}, \code{accessions}, \code{metadata}}{Inputs / record metadata.}
\item{\code{all_orfs}}{Combined \code{\link[=scan_orfs]{scan_orfs()}} table across genomes.}
\item{\code{noncanonical_orfs}}{Filtered subset used downstream.}
\item{\code{orf_families}}{Per-cluster summary (\code{cluster_id}, \code{n_genomes},
\code{n_orfs}, \code{longest_aa}, \code{longest_len}).}
\item{\code{presence_absence}}{Integer matrix \verb{[family x genome]} of 0/1.}
\item{\code{msa_concat}}{\link[Biostrings:XStringSet-class]{Biostrings::AAStringSet} of length \code{n_genomes} —
the concatenated per-family alignments.}
\item{\code{identity_matrix}}{Genome × genome \verb{\% identity} (symmetric;
diagonal = \code{100}).}
\item{\code{jaccard_matrix}}{Genome × genome Jaccard distance on
presence/absence (only when \code{also_jaccard = TRUE}; \code{0} =
identical family sets, \code{1} = disjoint).}
\item{\code{gap_fraction_matrix}}{Fraction of supermatrix positions where
at least one of the pair carries a gap — the lineage-specific
gain/loss signal not captured by \code{pairwise_complete} identity.}
\item{\code{tree}}{\code{hclust} on \code{as.dist(1 - identity_matrix/100)} (average
linkage).}
}
}
\description{
Builds a pairwise genome × genome \strong{identity matrix} from a concatenated
protein alignment of ORF families shared across a clade's mitogenomes, and
(optionally) a complementary \strong{Jaccard presence/absence} matrix.  The
identity matrix measures within-shared-family sequence divergence; the
Jaccard matrix measures lineage-specific ORF family gain/loss.  Defaults to
restricting to \strong{noncanonical ORFs} (i.e. ORFs whose 5' position does not
coincide with a CDS feature in the same strand), so the comparison reflects
the non-canonical coding repertoire rather than the standard 13 mt CDSs.
}
\details{
Pipeline: NCBI search for \code{taxon} mitogenomes → \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}} for
sequence + features → \code{\link[=scan_orfs]{scan_orfs()}} per genome under the (required)
\code{genetic_code} → noncanonical filter → \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} clustering
into ORF families → per-family MAFFT alignment → concatenated supermatrix →
pairwise identity + Jaccard.

Requires the \strong{MAFFT} binary on \code{PATH} for the per-family MSA step.
Families present in only one genome (singletons) are produced by the
clustering step \emph{only} when \code{min_identity} would group them — by default
\code{\link[=find_conserved_orfs]{find_conserved_orfs()}} keeps families with \verb{>= 2} genomes, so singletons
are not included; the comparison thus measures shared-ORF divergence.
}
\examples{
\dontrun{
res <- compare_clade_orfeomes(
  taxon          = "Hominidae",
  genetic_code   = "SGC1",
  refseq_only    = TRUE,
  min_orf_length = 60
)
res$identity_matrix
plot(res$tree, main = "Hominidae ORFeome (noncanonical) — identity tree")
}

}
\seealso{
\code{\link[=find_clade_conserved_orfs]{find_clade_conserved_orfs()}}, \code{\link[=find_conserved_orfs]{find_conserved_orfs()}},
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}
}
