% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_mito.R
\name{fetch_mito_genbank}
\alias{fetch_mito_genbank}
\title{Fetch Mitochondrial Genomes from NCBI RefSeq}
\usage{
fetch_mito_genbank(accessions, api_key = NULL, verbose = TRUE)
}
\arguments{
\item{accessions}{Character vector of sequence sources.  Each element may be:
\itemize{
\item An \strong{NCBI accession number} (e.g. \code{"NC_002333.2"}) — full GenBank
record is fetched, including the feature table.
\item A \strong{path to a FASTA file} — sequence is read locally; features will
be an empty \code{data.frame} (no annotation available from FASTA).
\item A \strong{raw nucleotide string} — treated as an anonymous sequence; features
will be an empty \code{data.frame}.
}

All three types may be mixed in a single call.}

\item{api_key}{Character scalar. NCBI API key for higher rate limits
(10 requests/s vs 3/s without a key).  Obtain one free at
\url{https://www.ncbi.nlm.nih.gov/account/}.  Set to \code{NULL} (default) to
use unauthenticated access.}

\item{verbose}{Logical.  Print progress messages?  Default \code{TRUE}.}
}
\value{
A named list with one entry per accession.  Each entry is itself
a list with:
\describe{
\item{\code{accession}}{The requested accession string.}
\item{\code{definition}}{DEFINITION line from the GenBank record.}
\item{\code{length}}{Sequence length in nucleotides.}
\item{\code{sequence}}{The complete mitogenome as a \link[Biostrings:DNAString-class]{Biostrings::DNAString}.}
\item{\code{features}}{A \code{data.frame} of parsed GenBank features with
columns \code{type}, \code{start}, \code{end}, \code{strand}, \code{gene}, \code{product}, and
\code{note}.  Locations are 1-based and inclusive; \code{strand} is \code{"+"} or
\code{"-"}.}
}
}
\description{
Downloads one or more mitochondrial genome records from NCBI by accession
number and parses both the nucleotide sequence and the GenBank feature
table.  The feature table is used downstream by
\code{\link[=annotate_genomic_regions]{annotate_genomic_regions()}} to label ORFs by genomic context.
}
\details{
Sequences are fetched via \code{rentrez::entrez_fetch()} in GenBank flat-file
format (\code{rettype = "gbwithparts"}).  A brief pause between requests
respects NCBI's rate limits even without an API key.

Feature locations are simplified to a single \verb{[start, end]} span:
\itemize{
\item \emph{complement} locations set \code{strand = "-"}.
\item \emph{join} locations use the overall min/max span.
\item Wrap-around locations (e.g. \verb{join(16500..16770,1..200)}) are handled
correctly for circular genomes.
}
}
\examples{
\dontrun{
genomes <- fetch_mito_genbank(c("NC_002333.2", "NC_028087.1"))

# Inspect Danio rerio features
head(genomes[["NC_002333.2"]]$features)

# Pass the sequence directly to find_orfs()
orfs <- find_orfs(genomes[["NC_002333.2"]]$sequence)
}

}
