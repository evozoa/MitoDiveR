% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_clade_conserved_orfs.R
\name{find_clade_conserved_orfs}
\alias{find_clade_conserved_orfs}
\title{Discover Conserved ORFs Across RefSeq Mitogenomes in a Taxon}
\usage{
find_clade_conserved_orfs(
  taxon,
  max_genomes = 20L,
  refseq_only = TRUE,
  min_length = 13000L,
  min_orf_length = 60L,
  genetic_code = .stop_no_code("genetic_code"),
  start_codons = c("ATG", "GTG", "ATA", "ATT", "ATC"),
  min_identity = 50,
  min_sequences = 0.5,
  api_key = NULL
)
}
\arguments{
\item{taxon}{Character scalar.  Taxon name (e.g. \code{"Sebastes"},
\code{"Nothobranchius"}, \code{"Primates"}) or an NCBI Taxonomy ID as a
character (e.g. \code{"6790"}).  Passed verbatim to \link[rentrez:rentrez-package]{rentrez::rentrez} as
\verb{<taxon>[ORGN]}.}

\item{max_genomes}{Integer.  Cap on the number of mitogenomes to
fetch and process.  Default \code{20L}.  Set to \code{Inf} for no cap.}

\item{refseq_only}{Logical.  Restrict the query to RefSeq deposits
(\code{srcdb_refseq[PROP]}).  Default \code{TRUE}.  Set \code{FALSE} to include
non-RefSeq GenBank mitogenomes — useful for clades with sparse
RefSeq coverage but introduces redundancy from multiple
submissions of the same species.}

\item{min_length}{Integer.  Minimum sequence length (nt) for a hit
to be considered a complete mitogenome.  Default \code{13000L}
(excludes partial vertebrate mtDNA deposits).}

\item{min_orf_length}{Integer.  Passed to \code{\link[=scan_orfs]{scan_orfs()}}.  Default
\code{60L}.}

\item{genetic_code}{Character scalar.  Genetic code identifier
passed to \code{\link[=scan_orfs]{scan_orfs()}}.  Required (no default) — e.g. \code{"SGC1"}
(vertebrate mitochondrial).}

\item{start_codons}{Character vector of recognised start codons.
Default \code{c("ATG","GTG","ATA","ATT","ATC")}.}

\item{min_identity}{Numeric in (0, 100].  Minimum percent amino-acid
identity for \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} clustering.  Default \code{50}.}

\item{min_sequences}{Numeric.  Minimum fraction of sequences (or
absolute count if \verb{>= 1}) in which a cluster must be present.
Default \code{0.5}.}

\item{api_key}{Optional NCBI API key (character).  If supplied, set
via \code{\link[rentrez:set_entrez_key]{rentrez::set_entrez_key()}} for the duration of the call to
raise the request limit from 3 to 10 requests per second.}
}
\value{
A list with components:
\describe{
\item{\code{taxon}}{The taxon string supplied.}
\item{\code{accessions}}{Character vector of accessions fetched.}
\item{\code{metadata}}{\code{data.frame} with one row per accession
(\code{acc}, \code{title}, \code{length}).}
\item{\code{all_orfs}}{The combined \code{\link[=scan_orfs]{scan_orfs()}} table.}
\item{\code{conserved}}{The \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} clustered table.}
}
}
\description{
Pulls all RefSeq complete mitogenomes annotated to a user-supplied
taxon (genus / family / order), scans each for ORFs, and clusters
the result with \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} to identify ORFs shared
across the clade.  Intended as a one-call entry point for clade-
level survey work.
}
\details{
Network access is required.  NCBI rate-limits unauthenticated
Entrez queries to three requests per second; set an API key via
\code{api_key} for higher throughput on large clades.  When \code{max_genomes}
caps the search, the first \code{max_genomes} accessions returned by the
Entrez query are used (typically ordered by deposit date,
newest-first).  For deterministic taxon sampling, supply a curated
accession list to \code{\link[=scan_orfs]{scan_orfs()}} + \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} directly.
}
\examples{
\dontrun{
# Conserved ORFs across Sebastes rockfishes
res <- find_clade_conserved_orfs("Sebastes", max_genomes = 20)
res$metadata
head(res$conserved)

# Larger primate survey under permissive RefSeq-or-GenBank query
primates <- find_clade_conserved_orfs(
  "Primates",
  max_genomes = 30,
  refseq_only = FALSE
)
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, \code{\link[=fetch_sequences]{fetch_sequences()}}
}
