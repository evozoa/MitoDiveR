% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_conserved_windows.R
\name{find_conserved_windows}
\alias{find_conserved_windows}
\title{Find Conserved Amino Acid Windows Across Mitochondrial Genomes}
\usage{
find_conserved_windows(
  genomes,
  genetic_codes = c("SGC0", "SGC1"),
  min_pct_identity = 40,
  min_window_aa = 10L,
  min_segment_aa = 30L,
  same_strand_only = TRUE,
  substitution_matrix = "BLOSUM62",
  gap_opening = 10,
  gap_extension = 4,
  verbose = TRUE
)
}
\arguments{
\item{genomes}{Named list of genome records as returned by
\code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}.  Must contain at least two elements.}

\item{genetic_codes}{Character vector of genetic-code identifiers passed to
\code{\link[=translate_all_frames]{translate_all_frames()}}.  Default \code{c("SGC0", "SGC1")}.}

\item{min_pct_identity}{Numeric.  Minimum percent identity of the local
alignment to report a window.  Default \code{40}.}

\item{min_window_aa}{Integer.  Minimum number of amino acid residues
(non-gap) that must be present in the reported aligned window.
Default \code{10L}.}

\item{min_segment_aa}{Integer.  Minimum length of a peptide segment
(inter-stop run) to be included in alignment.  Larger values reduce
runtime by excluding very short segments unlikely to produce informative
alignments.  Must be >= \code{min_window_aa}.  Default \code{30L}.
Set to \code{min_window_aa} for exhaustive search (slower).}

\item{same_strand_only}{Logical.  If \code{TRUE} (default), only compare
forward-strand segments of genome A with forward-strand segments of genome
B, and reverse-strand with reverse-strand.  Cross-strand comparisons are
biologically uncommon for conserved ORFs and roughly double runtime.  Set
to \code{FALSE} to enable exhaustive cross-strand search.}

\item{substitution_matrix}{Character.  Default \code{"BLOSUM62"}.}

\item{gap_opening}{Numeric.  Default \code{10}.}

\item{gap_extension}{Numeric.  Default \code{4}.}

\item{verbose}{Logical.  Print progress?  Default \code{TRUE}.}
}
\value{
A \code{data.frame} with one row per conserved window, sorted by
descending \code{pct_identity}, with columns:
\describe{
\item{\code{acc_a}, \code{acc_b}}{Accessions of the two genomes compared.}
\item{\code{code}}{Genetic code used for translation (e.g. \code{"SGC0"}).}
\item{\code{strand_a}, \code{frame_a}}{Strand (\code{"+"} / \code{"-"}) and frame (1–3) of
the window in genome A.}
\item{\code{strand_b}, \code{frame_b}}{Strand and frame in genome B.}
\item{\code{genome_start_a}, \code{genome_end_a}}{1-based genomic coordinates of
the window in genome A.}
\item{\code{genome_start_b}, \code{genome_end_b}}{1-based genomic coordinates in
genome B.}
\item{\code{window_aa_a}}{Amino acid sequence of the window in genome A
(no gaps).}
\item{\code{window_aa_b}}{Amino acid sequence of the window in genome B.}
\item{\code{window_length_aa}}{Number of aligned amino acid columns
(including gaps).}
\item{\code{pct_identity}}{Percent identity over the aligned window.}
\item{\code{alignment_score}}{Raw Smith-Waterman score.}
\item{\code{genomic_region_a}, \code{genomic_region_b}}{Genomic context from the
GenBank feature table (if features are present in the genome records).}
}
}
\description{
Compares full six-frame translations of two or more mitochondrial genomes
to identify windows of conserved amino acid sequence.  Unlike
\code{\link[=cluster_conserved_orfs]{cluster_conserved_orfs()}}, this approach does not require ORF calling:
the entire genome is translated as a continuous string under each genetic
code.  Each inter-stop peptide segment is then compared pairwise between
species, revealing all regions of inter-species homology — canonical ETC
genes, rRNA-embedded ORFs, D-loop peptides, and novel micropeptide
candidates alike.
}
\details{
Canonical protein-coding gene windows will typically appear at the top of
the output (highest identity); novel MDP candidates appear in non-CDS
regions.  Use the \code{genomic_region} column to stratify results.
}
\examples{
\dontrun{
genomes <- fetch_mito_genbank(c("NC_002333.2", "NC_011379.1"))
windows <- find_conserved_windows(genomes, min_pct_identity = 50)

# Novel candidates: not in canonical CDS
novel <- windows[!grepl("^CDS:", windows$genomic_region_a), ]

# Exhaustive search including very short segments (slower)
windows_all <- find_conserved_windows(genomes, min_segment_aa = 10L)
}

}
