% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_orf_orthologs.R
\name{find_orf_orthologs}
\alias{find_orf_orthologs}
\title{Find ORF Orthologs ("ORFologs") of a Known Peptide Across Taxa}
\usage{
find_orf_orthologs(
  query,
  taxa = NULL,
  genomes = NULL,
  host_region = NULL,
  method = c("local", "tblastn", "blastn"),
  genetic_code = .stop_no_code("genetic_code"),
  start_codons = c("ATG", "GTG", "ATA", "ATT", "ATC"),
  min_orf_length = 30L,
  min_identity = 40,
  min_coverage = 50,
  refseq_only = TRUE,
  max_genomes = 50L,
  flank = 150L,
  annotate = TRUE,
  api_key = NULL,
  poll_interval = 15L,
  timeout = 300L
)
}
\arguments{
\item{query}{The known ORF's amino-acid sequence as a character scalar.  May
be named (the name labels the query in the output); otherwise labelled
\code{"query_orf"}.}

\item{taxa}{Optional character vector of taxon names (e.g.
\code{c("Sebastes", "Danio")}) or NCBI Taxonomy IDs.  For \code{method = "local"}
their RefSeq mitogenomes are fetched and scanned; for the remote methods
they scope the BLAST search via \code{ENTREZ_QUERY}.}

\item{genomes}{Optional sequences to scan directly under \code{method = "local"},
in any form accepted by the package input resolver (a \code{DNAStringSet}, NCBI
accessions, a FASTA file, or raw text).  Bypasses NCBI discovery.}

\item{host_region}{Nucleotide sequence of the conserved host locus the ORF is
embedded in.  Required for \code{method = "blastn"} (used as the BLAST query);
ignored otherwise.}

\item{method}{Discovery strategy; one of \code{"local"}, \code{"tblastn"}, \code{"blastn"}.}

\item{genetic_code}{Character scalar genetic-code id passed to \code{\link[=scan_orfs]{scan_orfs()}}.
Required (no default).  For MDPs use \code{"SGC0"} (the cytoplasmic 80S ribosome
translates them), not the mitochondrial \code{"SGC1"}.}

\item{start_codons}{Character vector of recognised start codons.  Default is
the permissive mitochondrial set \code{c("ATG","GTG","ATA","ATT","ATC")}.}

\item{min_orf_length}{Integer.  Minimum ORF length (nt) for \code{\link[=scan_orfs]{scan_orfs()}}.
Default \code{30L} (~10 aa; MDPs are short).}

\item{min_identity}{Numeric (0–100).  Minimum percent identity of the
ORF-to-query alignment to report a hit.  Default \code{40}.}

\item{min_coverage}{Numeric (0–100).  Minimum percent of the query peptide
covered by the alignment.  Default \code{50}.  Suppresses short spurious motif
matches.}

\item{refseq_only}{Logical.  Constrain remote searches (and \code{taxa} discovery)
to RefSeq mitochondrion records.  Default \code{TRUE}.}

\item{max_genomes}{Integer.  Cap on genomes fetched (\code{taxa}) or hit subject
ranges harvested (remote methods).  Default \code{50L}.}

\item{flank}{Integer.  Nucleotides added either side of a harvested BLAST hit
range before scanning, so a start codon just outside the aligned segment is
not missed.  Default \code{150L}.  Remote methods only.}

\item{annotate}{Logical.  For accession-backed genomes, annotate each hit's
\code{genomic_region} via \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}} + \code{\link[=annotate_genomic_regions]{annotate_genomic_regions()}}.
Default \code{TRUE}.  Silently skipped when features are unavailable.}

\item{api_key}{Optional NCBI API key (raises the Entrez rate limit).}

\item{poll_interval, timeout}{Integers.  BLAST poll cadence and overall
timeout (seconds) for the remote methods.  Defaults \code{15L} and \code{300L}.}
}
\value{
A \code{data.frame} (invisibly empty if nothing is found) with one row per
confirmed ortholog ORF:
\describe{
\item{\code{query_name}}{Label of the query peptide.}
\item{\code{seq_id}}{Source genome / window identifier (accession when known).}
\item{\code{start},\code{end},\code{strand},\code{frame}}{ORF coordinates on \code{seq_id}.}
\item{\code{length_nt},\code{protein_sequence}}{ORF length and translation.}
\item{\code{pct_identity}}{Percent identity of the ORF-to-query alignment.}
\item{\code{query_coverage}}{Percent of the known query peptide covered by the
alignment (the orthology-relevant metric \code{min_coverage} gates on).}
\item{\code{orf_coverage}}{Percent of the discovered ORF covered by the
alignment.}
\item{\code{genomic_region}}{Host-feature label when annotation succeeded, else
\code{NA} (remote windows carry their BLAST provenance instead).}
\item{\code{method},\code{blast_accession},\code{blast_from},\code{blast_to},\code{blast_evalue}}{
Discovery provenance; BLAST columns are \code{NA} under \code{method = "local"}.}
}
}
\description{
Locates the ortholog of a known noncanonical open reading frame — typically a
mitochondria-derived peptide (MDP) — in other taxa whose mitogenomes are not
annotated for such ORFs.  Because these ORFs are never in the feature table,
the search is two-step: discover candidate coding stretches, then translate
them in all six frames (\code{\link[=scan_orfs]{scan_orfs()}}) and match each ORF to the query
peptide (\code{\link[=score_mdp_similarity]{score_mdp_similarity()}}).
}
\details{
Three discovery strategies (\code{method}) feed the same local confirmation core,
so the output table is identical regardless of how candidates were found:
\describe{
\item{\code{"local"}}{No remote search.  Scan whole RefSeq mitogenomes for a
clade (\code{taxa}) or a set you supply (\code{genomes}).  Each mitogenome is only
~16 kb, so scanning the entire genome is cheap; orthology is confirmed
positionally with \code{\link[=annotate_genomic_regions]{annotate_genomic_regions()}}.  Best when the taxonomic
scope is already known.}
\item{\code{"tblastn"}}{Remote, peptide-level discovery.  The \code{query} peptide is
submitted as a TBLASTN search (protein vs. translated nucleotide), the
most sensitive option across deep divergence.  Hit subject ranges are
harvested (± \code{flank}) and confirmed locally.}
\item{\code{"blastn"}}{Remote, host-window discovery.  A conserved nucleotide
\code{host_region} the ORF sits within (e.g. a stretch of 12S/16S rRNA) is
submitted as a BLASTN search.  Anchoring on the conserved host locus is
itself the positional/orthology filter.}
}

For the remote methods the search space is constrained to the RefSeq
mitochondrion set (\code{refseq_only}), which collapses taxa with vast sequence
holdings (e.g. \emph{Danio}) to a single curated ~16 kb mitogenome and dedupes to
one record per species — the same constraint that makes orthology meaningful.

This separates \emph{homology} (the peptide aligns) from \emph{orthology} (it aligns at
the corresponding locus).  Under \code{"local"}/\code{"blastn"} the positional anchor is
explicit — the annotated host region, or the conserved host window searched.
Under \code{"tblastn"} confirm orthology by inspecting \code{blast_accession}/\verb{blast_*}
against the expected host gene.  Remote methods require network access and
degrade with a clear error when NCBI is unreachable.
}
\examples{
\dontrun{
humanin <- c(Humanin = "MAPRGFSCLLLLTSEIDLPVKRRA")

# Defined clade: scan whole RefSeq mitogenomes, confirm positionally
hits <- find_orf_orthologs(humanin, taxa = "Sebastes",
                           method = "local", genetic_code = "SGC0")

# Open-ended discovery: peptide-level TBLASTN across RefSeq mitogenomes
hits <- find_orf_orthologs(humanin, method = "tblastn",
                           genetic_code = "SGC0")

# Host-anchored discovery: BLASTN a conserved 16S window the ORF rides on
hits <- find_orf_orthologs(humanin, host_region = my_16s_window,
                           method = "blastn", genetic_code = "SGC0")
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=score_mdp_similarity]{score_mdp_similarity()}}, \code{\link[=annotate_genomic_regions]{annotate_genomic_regions()}},
\code{\link[=find_clade_conserved_orfs]{find_clade_conserved_orfs()}}, \code{\link[=blast_orfs]{blast_orfs()}}
}
