% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_orfs.R
\name{find_orfs}
\alias{find_orfs}
\title{Find Open Reading Frames in a (Mitochondrial) Genome}
\usage{
find_orfs(
  sequence,
  genetic_code = "SGC0",
  start_codons = "ATG",
  min_orf_length = 100L,
  max_orf_length = Inf,
  both_strands = TRUE,
  circular = TRUE,
  include_stop_codon = TRUE
)
}
\arguments{
\item{sequence}{Nucleotide sequence to search. Accepts any of:
\itemize{
\item A raw nucleotide \code{character} string (case-insensitive)
\item A path to a FASTA file (single- or multi-sequence)
\item An NCBI accession number (e.g. \code{"NC_002333.2"}) — sequence is
fetched automatically via the NCBI API
\item A \link[Biostrings:DNAString-class]{Biostrings::DNAString} or \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet}
}

When the input resolves to multiple sequences (multi-record FASTA or
multi-sequence DNAStringSet), ORFs are searched in each sequence and
results are combined into a single \code{data.frame} with a \code{seq_id} column
identifying the source sequence.}

\item{genetic_code}{Character scalar. Biostrings genetic-code identifier
passed to \code{\link[Biostrings:GENETIC_CODE]{Biostrings::getGeneticCode()}}. Defaults to \code{"SGC0"}
(standard vertebrate nuclear code, NCBI table 1). Use \code{"SGC1"} for the
vertebrate mitochondrial code (TGA = Trp; AGA/AGG = stop).}

\item{start_codons}{Character vector of codons that initiate an ORF.
Defaults to \code{"ATG"}. For vertebrate mitochondrial genomes you may wish
to add alternative starts: \code{c("ATG", "GTG", "ATT", "ATC", "ATA")}.}

\item{min_orf_length}{Integer. Minimum ORF length in nucleotides,
measured from the first base of the start codon to the last base of
the stop codon (inclusive when \code{include_stop_codon = TRUE}).
Default \code{100L}.}

\item{max_orf_length}{Integer. Maximum ORF length in nucleotides
(same measurement as \code{min_orf_length}). ORFs longer than this value
are silently excluded. Default \code{Inf} (no upper limit).}

\item{both_strands}{Logical. Search the reverse-complement strand as well
as the forward strand? Default \code{TRUE}.}

\item{circular}{Logical. Treat the sequence as a circular molecule and
detect ORFs that span the linearisation boundary? Default \code{TRUE}.}

\item{include_stop_codon}{Logical. Include the stop codon in the reported
\code{orf_sequence} and \code{length_nt}? Default \code{TRUE}. When \code{FALSE}, the stop
codon column is still reported but the sequence/length exclude it.}
}
\value{
A \code{data.frame} sorted by \code{start} then \code{strand}, with columns:
\describe{
\item{\code{strand}}{\code{"+"} or \code{"-"}.}
\item{\code{frame}}{Reading frame: \code{+1}, \code{+2}, \code{+3}, \code{-1}, \code{-2}, or \code{-3}.
On the minus strand, frame \code{-1} starts from the last nucleotide of
the genome, \code{-2} from the second-to-last, etc.}
\item{\code{start}}{1-based genomic start coordinate (always \code{<=} \code{end}
unless \code{wraps_around} is \code{TRUE}).}
\item{\code{end}}{1-based genomic end coordinate (inclusive).}
\item{\code{length_nt}}{ORF length in nucleotides.}
\item{\code{length_aa}}{Number of residues in the translated sequence
(including \code{"*"} for the stop codon when \code{include_stop_codon = TRUE}).}
\item{\code{start_codon}}{Initiating codon.}
\item{\code{stop_codon}}{Terminating codon (\code{NA} when
\code{include_stop_codon = FALSE}).}
\item{\code{wraps_around}}{\code{TRUE} when the ORF spans the origin of a
circular genome (only possible when \code{circular = TRUE}).}
\item{\code{orf_sequence}}{Nucleotide sequence of the ORF on the sense
strand.}
\item{\code{protein_sequence}}{Amino-acid translation. The first residue
reflects the actual start codon (not forced to Met); subsequent
residues follow the selected genetic code.}
}

Returns an empty \code{data.frame} (invisibly) when no ORFs are found.
}
\description{
Searches all six reading frames (three forward, three reverse-complement)
of a nucleotide sequence and returns every ORF — defined as a run from a
recognised start codon to the first in-frame stop codon — that meets the
minimum length threshold.
}
\details{
\subsection{Genetic codes}{

The vertebrate mitochondrial code (\code{"SGC1"}) differs from the standard
code: TGA = Trp; AGA = stop; AGG = stop; ATA = Met.  Always choose the
code matching the taxon whose mitogenome you are analysing — results
obtained with the wrong code will include spurious truncations (stop
codons read through) or missed ORFs.
}

\subsection{Circular genomes}{

When \code{circular = TRUE} the genome is temporarily duplicated.  An ORF is
retained only when its start codon lies in the first copy (positions
1–\emph{L}), eliminating wholly duplicated hits while capturing boundary-
spanning ORFs.  For such ORFs \code{wraps_around = TRUE} and \code{end < start}.
}

\subsection{Overlapping ORFs}{

All ORFs meeting the length threshold are reported, including nested and
overlapping entries within the same reading frame.  To retain only the
longest non-overlapping set, filter the returned table downstream.
}
}
\examples{
\dontrun{
library(Biostrings)

# Quick synthetic test (standard code, ATG only, linear)
seq <- paste0("ATGAAACCCGGGTTTTAA", strrep("A", 50))
orfs <- find_orfs(seq,
                  genetic_code   = "SGC0",
                  start_codons   = "ATG",
                  min_orf_length = 15,
                  circular       = FALSE)
print(orfs[, c("strand","frame","start","end","length_nt","protein_sequence")])

# Real mitochondrial genome from a FASTA file
mito <- readDNAStringSet("my_mito.fasta")[[1]]
orfs <- find_orfs(mito)
}

}
