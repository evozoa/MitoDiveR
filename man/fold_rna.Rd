% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold_rna.R
\name{fold_rna}
\alias{fold_rna}
\title{Predict RNA Secondary Structure}
\usage{
fold_rna(sequence, temperature = 37, circular = FALSE, constraints = NULL)
}
\arguments{
\item{sequence}{A nucleotide sequence as a character string, or a
\link[Biostrings:DNAString-class]{Biostrings::DNAString}, \link[Biostrings:RNAString-class]{Biostrings::RNAString}, or
\link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} / \link[Biostrings:XStringSet-class]{Biostrings::RNAStringSet} object.  If a
\code{StringSet} is supplied, each element is folded separately and a list of
results is returned.  Ambiguity codes are passed through to \code{RNAfold}
unchanged.}

\item{temperature}{Numeric.  Folding temperature in °C.  Default \code{37}.}

\item{circular}{Logical.  Treat the sequence as circular (uses
\code{--circ} flag).  Default \code{FALSE}.}

\item{constraints}{Character string of dot-bracket constraints to enforce
(same length as sequence; \code{.} = unconstrained, \code{x} = unpaired,
\code{<}/\code{>} = paired).  \code{NULL} for unconstrained folding (default).}
}
\value{
For a single sequence, a list of class \code{"rna_fold"} with:
\describe{
\item{\code{sequence}}{The RNA sequence (U-alphabet) that was folded.}
\item{\code{structure}}{Dot-bracket secondary structure string.}
\item{\code{mfe}}{Minimum free energy (kcal/mol, numeric).}
\item{\code{base_pairs}}{\code{data.frame} with columns \code{pos5} and \code{pos3} giving
the 1-based positions of each base pair.}
\item{\code{n_pairs}}{Total number of base pairs.}
\item{\code{n_hairpins}}{Number of hairpin (stem-loop) motifs.}
\item{\code{length_nt}}{Sequence length (nt).}
}
For a \code{StringSet} input, a named list of such objects.
}
\description{
Folds a nucleotide sequence into its minimum free-energy (MFE) secondary
structure using ViennaRNA's \code{RNAfold} program, which implements the same
Turner-energy-based algorithm as Mfold and UNAFold.  DNA sequences are
automatically converted to RNA (T → U) before folding.
}
\details{
\subsection{Dependency}{

\code{fold_rna()} requires the \strong{ViennaRNA} package (\code{RNAfold} binary) to be
installed and on the system \code{PATH}.  ViennaRNA is freely available at
\url{https://www.tbi.univie.ac.at/RNA/} and via conda (\verb{conda install -c bioconda viennarna}) or apt (\verb{sudo apt-get install vienna-rna}).
}

\subsection{Algorithm}{

\code{RNAfold} minimises the Gibbs free energy of the structure using the
nearest-neighbour Turner energy parameters, identical in principle to the
Mfold / UNAFold / RNAstructure family of programs.  Only the MFE structure
is returned; for the full partition function / base-pair probabilities use
\code{RNAfold -p} directly.
}
}
\examples{
\dontrun{
# Fold a single ORF nucleotide sequence
fold <- fold_rna("ATGAAACCCGGGTTTTAA")
fold$structure
fold$mfe
fold$n_hairpins

# Fold at a different temperature
fold_rna("ATGAAACCCGGGTTTTAA", temperature = 25)

# Fold all ORF sequences from scan_orfs() output
orfs  <- scan_orfs(seqs, min_orf_length = 60)
folds <- fold_rna(Biostrings::DNAStringSet(
           setNames(orfs$orf_sequence, orfs$seq_id)))
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}
}
