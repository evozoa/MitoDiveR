% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_rna_structure.R
\name{longest_stem}
\alias{longest_stem}
\title{Find the longest contiguous base-pair stem in a structure}
\usage{
longest_stem(structure, max_bulge = 0L)
}
\arguments{
\item{structure}{Dot-bracket structure string (single character; see
\code{\link[=fold_rna]{fold_rna()}}).}

\item{max_bulge}{Integer >= 0.  Maximum number of unpaired residues
allowed on either the 5' or 3' strand between two consecutive base
pairs that are still considered part of the same stem.  Default \code{0}
(strict — no bulges, only perfect ladders).  Set to \code{2}–\code{3} for
biologically realistic stem detection.}
}
\value{
A list with elements:
\describe{
\item{\code{stem_length}}{Integer.  Number of base pairs in the longest
stem (counting through allowed bulges).}
\item{\code{stem_5p_pos}}{Integer.  1-based 5' position of the first base
pair of the stem, or \code{NA} if no stem found.}
\item{\code{stem_3p_pos}}{Integer.  1-based 3' position of the partner of
the first base pair (i.e., the outermost 3' end of the stem).}
\item{\code{stem_span_nt}}{Integer.  Number of nucleotides between
\code{stem_5p_pos} and \code{stem_3p_pos} inclusive (the "footprint" of the
stem on the sequence).}
}
}
\description{
Identifies the longest consecutive base-pair "ladder" in a dot-bracket
structure — i.e., the longest run of positions where \verb{i, i+1, i+2, ...}
on the 5' side pair with \verb{j, j-1, j-2, ...} on the 3' side, defining a
true stem rather than aggregate base-pair counts.
}
\details{
Optionally tolerates small bulges (single-strand insertions of up to
\code{max_bulge} residues on either strand) so that biologically realistic
stems with internal loops are still captured as a single stem.  This is
the recommended setting for detecting long inverted-repeat-driven
hairpins such as those found in mammalian ASncmtRNAs (Burzio lab; Gao
et al. 2017), where the diagnostic feature is a megahairpin of >=40
paired residues, typically interrupted by a few small bulges.

The strict mode (\code{max_bulge = 0}) requires perfect consecutive pairing.
This is overly restrictive for biological RNAs because real stems
typically contain small bulges.  For most asmtRNA / lncRNA hairpin
detection use \code{max_bulge = 2L} or \code{3L}.

Note that the longest stem may not be the most thermodynamically stable
or biologically functional one — for those questions consult
\code{fold_rna()$mfe} and the full base-pair table.  This function is
designed for \emph{topology} inference (presence/absence of long
inverted-repeat-driven megahairpins), not energetics.
}
\examples{
# Strict stem detection — perfect ladder
s1 <- longest_stem("(((((....)))))")
s1$stem_length    # 5

# Two stems separated by an unpaired junction:
#  ((((..(((....))))..))))
s2 <- longest_stem("((((..(((....))))..))))")
s2$stem_length    # 4 (the outer stem)

# Permissive mode handles small bulges in a single conceptual stem:
#  (((((.((....)).)))))
s3 <- longest_stem("(((((.((....)).)))))", max_bulge = 1L)
s3$stem_length    # 7 (bulged stem extends through the .((..)).)

\dontrun{
# Test for asmtRNA-class megahairpin in a folded transcript
f <- fold_rna(my_lstrand_rna_seq)
s <- longest_stem(f$structure, max_bulge = 3L)
is_megahairpin <- s$stem_length >= 40L
}

}
\seealso{
\code{\link[=fold_rna]{fold_rna()}}, \code{\link[=pair_partners]{pair_partners()}}
}
