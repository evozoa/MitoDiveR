% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_rna_structure.R
\name{pair_partners}
\alias{pair_partners}
\title{Map base-pair partners from a dot-bracket structure}
\usage{
pair_partners(structure)
}
\arguments{
\item{structure}{A character string of dot-bracket notation
(\code{(}, \verb{)}, \code{.}).  Must be balanced.  May come from \code{fold_rna()$structure}
or any other RNA-folding output.}
}
\value{
An integer vector of length \code{nchar(structure)}. Position \code{i} holds
the partner position of base \code{i}, or \code{NA_integer_} if base \code{i} is
unpaired (\code{.}) or pseudoknotted to a partner outside this notation.
}
\description{
Walks a dot-bracket secondary-structure string (as produced by
\code{\link[=fold_rna]{fold_rna()}}) and returns, for each position, the index of its
base-pair partner — or \code{NA} if the position is unpaired.
}
\examples{
# Simple stem-loop: ((((....))))
partners <- pair_partners("((((....))))")
partners       # 12 11 10  9 NA NA NA NA  4  3  2  1

\dontrun{
# Pair with fold_rna()
f <- fold_rna("GGGAAACCC")
pair_partners(f$structure)
}

}
\seealso{
\code{\link[=fold_rna]{fold_rna()}}, \code{\link[=longest_stem]{longest_stem()}}
}
