% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_conserved_orfeome.R
\name{plot_conserved_orfeome}
\alias{plot_conserved_orfeome}
\title{Circos plot of an ORFeome with conserved ORFs highlighted}
\usage{
plot_conserved_orfeome(
  windows,
  acc,
  genetic_code = .stop_no_code("genetic_code"),
  min_orf_length = 30L,
  highlight_identity = 95,
  highlight_color = "#7a0177",
  title = NULL,
  ...
)
}
\arguments{
\item{windows}{A \code{data.frame} returned by \code{\link[=find_conserved_windows]{find_conserved_windows()}}.}

\item{acc}{Accession of the genome to plot.  Conserved windows are matched
wherever \code{acc} appears, whether as \code{acc_a} or \code{acc_b}, using the
corresponding genome coordinates, so either member of a pair can be
plotted directly.}

\item{genetic_code}{Genetic-code identifier passed to \code{\link[=scan_orfs]{scan_orfs()}}.
Required (no default) — use \code{"SGC1"} for canonical mito genes or \code{"SGC0"}
for a noncanonical/MDP scan.}

\item{min_orf_length}{Integer minimum ORF length in nt, passed to
\code{\link[=scan_orfs]{scan_orfs()}}.  Default \code{30L}.}

\item{highlight_identity}{Minimum percent identity for the self-match that
outlines a shared ORF, passed as \code{mdp_min_identity}.  Keep high
(default \code{95}) so shared ORFs light up while the built-in human MDP
reference set stays quiet.}

\item{highlight_color}{Outline/label colour for shared ORFs.  Default
\code{"#7a0177"}.}

\item{title}{Plot title.  Default is derived from \code{acc}.}

\item{...}{Further arguments forwarded to \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}} (e.g.
\code{file}, \code{collapse}, \code{frame_colors}, \code{track_height}).}
}
\value{
Invisibly, the scanned ORF \code{data.frame} with an added logical
\code{shared} column.  Called mainly for the side effect (the plot).
}
\description{
Convenience wrapper that turns a \code{\link[=find_conserved_windows]{find_conserved_windows()}} result straight
into a \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}} figure for one genome.  It fetches the
genome, scans its full ORFeome, flags each ORF as \strong{shared} (overlapping a
conserved window) or \strong{unique}, and draws the six-frame circos plot with
the shared ORFs outlined and labelled.
}
\details{
Reading frame is encoded by the concentric rings (as in
\code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}}); conservation is encoded by reusing that function's
MDP-highlight mechanism: the shared ORFs' own protein sequences are passed
as \code{custom_mdps}, so each shared ORF self-matches and is outlined in
\code{highlight_color}.  Unique ORFs appear as plain frame-coloured bars.

By default the plot is drawn to the active graphics device (it "pops up").
Pass \code{file = "x.png"} (forwarded via \code{...}) to write a file instead.
}
\examples{
\dontrun{
genomes <- fetch_mito_genbank(c("MK848690.1", "KT289925.1"))
windows <- find_conserved_windows(genomes, genetic_codes = "SGC0")

# Pops up on screen; shared ORFs outlined, frame = ring
plot_conserved_orfeome(windows, acc = "MK848690.1", genetic_code = "SGC0")

# The other side of the pair, written to a file
plot_conserved_orfeome(windows, acc = "KT289925.1", genetic_code = "SGC0",
                       file = "kt289925_orfeome.png")
}

}
\seealso{
\code{\link[=find_conserved_windows]{find_conserved_windows()}}, \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}},
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}.
}
