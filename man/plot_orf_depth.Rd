% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_orf_depth.R
\name{plot_orf_depth}
\alias{plot_orf_depth}
\title{Per-reading-frame ORF depth across a taxon}
\usage{
plot_orf_depth(
  taxon,
  genetic_code = .stop_no_code("genetic_code"),
  reference = NULL,
  depth_unit = c("species", "orf"),
  noncanonical_only = TRUE,
  max_genomes = 20L,
  refseq_only = TRUE,
  min_length = 13000L,
  min_orf_length = 60L,
  start_codons = c("ATG", "GTG", "ATA", "ATT", "ATC"),
  annotate_genes = TRUE,
  frame_colors = NULL,
  mafft_bin = "mafft",
  mafft_opts = "--auto",
  title = NULL,
  file = NULL,
  api_key = NULL
)
}
\arguments{
\item{taxon}{Character scalar.  Taxon name searched as \verb{<taxon>[ORGN]}
(e.g. \code{"Sebastes"}, \code{"Cyprinidae"}).}

\item{genetic_code}{Genetic-code identifier passed to \code{\link[=scan_orfs]{scan_orfs()}}.
Required (no default).  Use \code{"SGC0"} for a noncanonical/MDP scan or
\code{"SGC1"} for canonical vertebrate-mitochondrial genes.}

\item{reference}{Optional accession (must be one of the fetched genomes)
whose coordinates define the depth axis.  \code{NULL} (default) uses the
longest genome.}

\item{depth_unit}{\code{"species"} (default) counts each species at most once per
position per frame (genomes are deduplicated by organism); \code{"orf"} is a
raw ORF pileup where every ORF adds one and no deduplication is done.}

\item{noncanonical_only}{Logical.  If \code{TRUE} (default), drop ORFs whose 5'
end matches an annotated CDS (via the same test as
\code{\link[=compare_clade_orfeomes]{compare_clade_orfeomes()}}), keeping the maps focused on noncanonical ORFs.}

\item{max_genomes}{Integer.  Maximum genomes to fetch.  Default \code{20L}.}

\item{refseq_only}{Logical.  Restrict the NCBI search to RefSeq
(\code{srcdb_refseq[PROP]}).  Default \code{TRUE}.}

\item{min_length}{Integer.  Minimum genome length (nt) to retain.
Default \code{13000L}.}

\item{min_orf_length}{Integer.  Minimum ORF length (nt) for \code{\link[=scan_orfs]{scan_orfs()}}.
Default \code{60L}.}

\item{start_codons}{Character vector of recognised start codons.}

\item{annotate_genes}{Logical.  Draw a reference gene ruler (CDS/rRNA/tRNA)
beneath the bottom track.  Default \code{TRUE}.}

\item{frame_colors}{Optional named character vector of six colours keyed by
\verb{"1","2","3","-1","-2","-3"}.  \code{NULL} uses the \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}}
palette.}

\item{mafft_bin}{Path to the MAFFT binary.  Default \code{"mafft"} (must be on
\code{PATH}).}

\item{mafft_opts}{Character options passed to MAFFT.  Default \code{"--auto"}.}

\item{title}{Optional plot title.  \code{NULL} derives one from the inputs.}

\item{file}{Optional output path ending in \code{.png} or \code{.pdf}.  \code{NULL}
(default) draws to the active device.}

\item{api_key}{Optional NCBI API key.}
}
\value{
Invisibly, a list with components \code{depth} (a numeric matrix
\verb{reference_length x 7}; columns \verb{+1,+2,+3,-1,-2,-3,sum}), \code{reference}
(accession used), \code{n_species}, \code{meta} (the genome table), and \code{params}.
}
\description{
Taxon-first companion to \code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}}.  Fetches every mitogenome
in a taxon, scans each for ORFs, aligns the genomes to a reference, maps ORF
coordinates onto reference base pairs, and draws \strong{seven stacked depth
tracks}: one per signed reading frame (\verb{+1, +2, +3, -1, -2, -3}) and a
seventh summing the six.  Each track shows, at every reference position, how
many species (or ORFs) carry an ORF in that frame there.  A peak in a frame
track flags a reference locus where the same noncanonical ORF recurs across
species — a conserved-ORF candidate.
}
\details{
Cross-species positions are made comparable by a reference-anchored
alignment: all genomes are aligned with MAFFT and each ORF's coordinates are
projected onto the chosen reference genome's base pairs (columns inserted
relative to the reference fold onto the preceding reference position).  The
depth profile is therefore in homologous coordinates, not raw bp.
}
\section{External tools}{

Requires MAFFT on \code{PATH} (as for \code{\link[=compare_clade_orfeomes]{compare_clade_orfeomes()}}) and network
access to NCBI.
}

\examples{
\dontrun{
# Seven depth tracks of noncanonical ORFs across rockfishes, on screen
plot_orf_depth("Sebastes", genetic_code = "SGC0")

# Raw ORF pileup, saved to a file, anchored on a chosen reference
res <- plot_orf_depth("Sebastes", genetic_code = "SGC0",
                      depth_unit = "orf", reference = "NC_005450.1",
                      file = "sebastes_orf_depth.png")
str(res$depth)
}

}
\seealso{
\code{\link[=plot_orfeome_circos]{plot_orfeome_circos()}}, \code{\link[=compare_clade_orfeomes]{compare_clade_orfeomes()}},
\code{\link[=find_clade_conserved_orfs]{find_clade_conserved_orfs()}}, \code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}.
}
