% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_orfeome_circos.R
\name{plot_orfeome_circos}
\alias{plot_orfeome_circos}
\title{Circos plot of a six-frame ORFeome}
\usage{
plot_orfeome_circos(
  orfs,
  genome_length,
  genes = NULL,
  seq_id = NULL,
  file = NULL,
  title = NULL,
  frame_colors = NULL,
  label_genes = c("CDS", "rRNA"),
  track_height = 0.07
)
}
\arguments{
\item{orfs}{A \code{data.frame} of ORFs with integer \code{frame} (\verb{1,2,3,-1,-2,-3}),
\code{start}, \code{end}, and (optionally) \code{wraps_around} columns — as returned by
\code{\link[=scan_orfs]{scan_orfs()}}.  If it has a \code{seq_id} column with more than one value, pass
\code{seq_id} to select one.}

\item{genome_length}{Integer length of the (circular) genome in nt.}

\item{genes}{Optional \code{data.frame} of canonical features for the central
ring, with \code{start}, \code{end}, and a label column (\code{gene} or \code{product}); a
\code{type} column (e.g. \code{CDS}, \code{rRNA}, \code{tRNA}, \code{D-loop}) is used for colouring
when present.  Pass \code{fetch_mito_genbank(acc)[[acc]]$features}.}

\item{seq_id}{Optional value to subset \code{orfs$seq_id} when multiple sequences
are present.}

\item{file}{Optional output path ending in \code{.png} or \code{.pdf}.  If \code{NULL} the
plot is drawn on the current graphics device.}

\item{title}{Optional plot title.}

\item{frame_colors}{Named character vector of six colours keyed by
\verb{"1","2","3","-1","-2","-3"}.  Defaults to blues (forward) / reds (reverse).}

\item{label_genes}{Character vector of feature \code{type}s to label (others are
drawn but unlabelled, to reduce clutter).  Default \code{c("CDS","rRNA")}.}

\item{track_height}{Height of each ORF ring (fraction of radius).  Default
\code{0.07}.}
}
\value{
Invisibly \code{NULL}; called for its side effect (the plot).
}
\description{
Renders the ORFs returned by \code{\link[=scan_orfs]{scan_orfs()}} / \code{\link[=scan_orfeome]{scan_orfeome()}} as six
concentric rings — one per reading frame (\verb{+1, +2, +3} then \verb{-1, -2, -3},
outermost to innermost) — arranged \emph{outside} a central ring that shows the
nucleotide coordinate axis and the canonical gene annotation.  This is the
genome-first companion view: every noncanonical ORF is placed in its frame
and strand context against the standard mitochondrial gene map.
}
\details{
Pure plotting: it does no network access.  Fetch the genome/annotation
yourself (e.g. with \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}) and pass \code{genome_length} and
\code{genes}.

Requires the \strong{circlize} package.  ORFs flagged \code{wraps_around} (spanning the
circular origin) are drawn as two arcs.  Rings are labelled at the top gap.
}
\examples{
\dontrun{
g    <- fetch_mito_genbank("NC_012920.1")[["NC_012920.1"]]
orfs <- scan_orfs(Biostrings::DNAStringSet(g$sequence),
                  min_orf_length = 150, circular = TRUE)
plot_orfeome_circos(orfs, genome_length = g$length, genes = g$features,
                    title = "Human rCRS ORFeome", file = "orfeome.png")
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=scan_orfeome]{scan_orfeome()}}, \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}
}
