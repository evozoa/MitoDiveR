% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_tmhmm.R
\name{predict_tmhmm}
\alias{predict_tmhmm}
\title{Predict Transmembrane Topology with DeepTMHMM}
\usage{
predict_tmhmm(x, ids = NULL, biolib_bin = "biolib")
}
\arguments{
\item{x}{Either a \strong{character vector} of protein sequences or the
\strong{\code{data.frame}} returned by \code{\link[=find_orfs]{find_orfs()}}.  Stop-codon asterisks
(\code{"*"}) are stripped automatically before submission.}

\item{ids}{Character vector of sequence identifiers, one per sequence.
Ignored when \code{x} is a \code{data.frame} (row indices are used).  If \code{NULL}
and \code{x} is a character vector, identifiers default to \code{seq_1}, \code{seq_2},
...}

\item{biolib_bin}{Path to the \code{biolib} executable.  Defaults to
\code{"biolib"}, assuming it is on \code{PATH}.  Install with:
\verb{pip install pybiolib}.}
}
\value{
A \code{data.frame} with one row per input sequence and the following
columns:
\describe{
\item{\code{seq_id}}{Sequence identifier.}
\item{\code{seq_length}}{Number of amino acids submitted.}
\item{\code{topology_class}}{Overall predicted class:
\code{"TM"} (alpha-helical transmembrane),
\code{"SP"} (signal peptide, no TM),
\code{"SP+TM"} (signal peptide plus TM helices),
\code{"BETA"} (beta-barrel membrane protein), or
\code{"GLOB"} (globular / soluble).}
\item{\code{n_tm_segments}}{Number of predicted TM segments (helices or
beta-strands).}
\item{\code{topology_string}}{Per-residue label string returned by
DeepTMHMM: \code{M} = membrane helix, \code{i} = inside (cytoplasmic),
\code{o} = outside (lumenal/IMS), \code{S} = signal peptide, \code{B} = beta-barrel
strand.}
}
When \code{x} is a \code{\link[=find_orfs]{find_orfs()}} \code{data.frame} these columns are appended to
the right of the original table.

The returned object also carries a \code{"domains"} attribute: a long-format
\code{data.frame} of per-residue domain boundaries parsed from the DeepTMHMM
GFF3 output.  Access it with \code{\link[=tmhmm_domains]{tmhmm_domains()}}.
}
\description{
Submits protein sequences to DeepTMHMM (via the local \code{biolib} CLI) and
returns a tidy summary of predicted transmembrane topology.  Accepts either
a plain character vector of sequences or the \code{data.frame} returned by
\code{\link[=find_orfs]{find_orfs()}}; in the latter case, results are merged back onto the ORF
table so that every ORF — canonical or novel — carries a topology
annotation.
}
\details{
\subsection{Requirements}{

Install the BioLib CLI once into any Python ≥ 3.8 environment visible
to R:

\if{html}{\out{<div class="sourceCode">}}\preformatted{pip install pybiolib
}\if{html}{\out{</div>}}

The first run downloads the DeepTMHMM model weights (~100 MB) into a
local cache; subsequent runs are faster.  A free BioLib account is
required for authenticated runs; create one at \url{https://biolib.com}.
Run \verb{biolib login} in a terminal before the first use.
}

\subsection{Why DeepTMHMM over TMHMM 2.0?}{

DeepTMHMM (Hallgren \emph{et al.}, 2022) jointly predicts alpha-helical TM
proteins, beta-barrel outer membrane proteins, and signal peptides in a
single model, achieving higher accuracy than TMHMM 2.0, particularly for
short and non-canonical sequences — the primary target of this package.
}
}
\examples{
\dontrun{
library(Biostrings)

# Annotate all ORFs from a mitogenome in one pipeline
mito <- readDNAStringSet("my_mito.fasta")[[1]]
orfs <- find_orfs(mito)
topo <- predict_tmhmm(orfs)

# Inspect only TM-containing ORFs
topo[topo$topology_class \%in\% c("TM", "SP+TM"), ]

# View domain boundaries for every sequence
tmhmm_domains(topo)
}

}
\references{
Hallgren J, Tsirigos KD, Pedersen MD, \emph{et al.} (2022) DeepTMHMM predicts
alpha and beta transmembrane proteins using a deep neural network.
\emph{bioRxiv}. \url{https://doi.org/10.1101/2022.04.08.487609}
}
\seealso{
\code{\link[=tmhmm_domains]{tmhmm_domains()}} to extract per-residue domain boundaries.
}
