% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_results.R
\name{save_results}
\alias{save_results}
\title{Save MitoDiveR Pipeline Results to Disk}
\usage{
save_results(
  orfs = NULL,
  conserved = NULL,
  mdp_hits = NULL,
  outdir = ".",
  prefix = "MitoDiveR",
  timestamp = TRUE
)
}
\arguments{
\item{orfs}{\code{data.frame} returned by \code{\link[=scan_orfs]{scan_orfs()}}, or \code{NULL} to skip.}

\item{conserved}{\code{data.frame} returned by \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, or
\code{NULL} to skip.}

\item{mdp_hits}{\code{data.frame} returned by \code{\link[=score_mdp_similarity]{score_mdp_similarity()}}, or
\code{NULL} to skip.}

\item{outdir}{Character scalar.  Directory to write files into.  Created
recursively if it does not exist.  Defaults to the current working
directory (\code{"."}).}

\item{prefix}{Character scalar.  Prefix prepended to every output filename.
Default \code{"MitoDiveR"}.  A timestamp is appended automatically so that
repeated runs do not overwrite previous results.}

\item{timestamp}{Logical.  Append a \code{YYYYMMDD_HHMMSS} timestamp to
filenames?  Default \code{TRUE}.  Set to \code{FALSE} for reproducible filenames
(will overwrite previous results).}
}
\value{
A named character vector of the file paths written, invisibly.
}
\description{
Writes one or more \code{data.frame} outputs from the MitoDiveR pipeline to
CSV files in a single call.  Any combination of the three main outputs
(\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, \code{\link[=score_mdp_similarity]{score_mdp_similarity()}}) can be
supplied; only non-\code{NULL} arguments are written.
}
\examples{
\dontrun{
seqs      <- fetch_sequences(c("NC_012920.1", "NC_001643.1"))
orfs      <- scan_orfs(seqs, min_orf_length = 60, max_orf_length = 900)
conserved <- find_conserved_orfs(orfs, min_identity = 50)
hits      <- score_mdp_similarity(conserved)

# Save all three outputs to ~/results/
save_results(orfs, conserved, hits, outdir = "~/results")

# Save with a fixed filename (no timestamp)
save_results(orfs, conserved, hits,
             outdir    = "~/results",
             prefix    = "NC012920_vs_NC001643",
             timestamp = FALSE)

# Save only the MDP hits
save_results(mdp_hits = hits, outdir = "~/results")
}

}
