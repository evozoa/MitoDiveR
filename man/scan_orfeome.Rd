% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_orfeome.R
\name{scan_orfeome}
\alias{scan_orfeome}
\title{Scan Sequences for ORFs Across Multiple Parameter Combinations}
\usage{
scan_orfeome(
  sequences,
  genetic_codes = .stop_no_code("genetic_codes"),
  min_orf_lengths = 60L,
  start_codon_sets = list(canonical = "ATG", permissive = c("ATG", "GTG", "ATA", "ATT",
    "ATC")),
  both_strands = TRUE,
  circular = TRUE,
  include_stop_codon = TRUE
)
}
\arguments{
\item{sequences}{NCBI accession(s), path to a FASTA file, raw
sequence string, or \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet}.  Accessions are
passed to \code{\link[=fetch_sequences]{fetch_sequences()}}.}

\item{genetic_codes}{Character vector of Biostrings genetic-code
identifiers.  Required (no default) — e.g. \code{c("SGC1", "SGC0")} to test
both the vertebrate mitochondrial and standard codes.}

\item{min_orf_lengths}{Integer vector of minimum ORF lengths in
nucleotides.  Default \code{60L} (single value).  Provide multiple
values (e.g. \code{c(60L, 90L, 150L)}) to scan at several thresholds.}

\item{start_codon_sets}{List of character vectors, each a set of
recognised start codons.  Default \code{list(canonical = "ATG", permissive = c("ATG","GTG","ATA","ATT","ATC"))}.}

\item{both_strands}{Logical. Search the reverse-complement strand?
Default \code{TRUE}.}

\item{circular}{Logical. Treat sequences as circular molecules?
Default \code{TRUE}.}

\item{include_stop_codon}{Logical. Include the stop codon in
reported sequences and lengths?  Default \code{TRUE}.}
}
\value{
A \code{data.frame} combining all \code{\link[=scan_orfs]{scan_orfs()}} outputs with
additional columns identifying each parameter combination:
\code{genetic_code} (the code identifier), \code{min_orf_length},
\code{start_codon_set} (name of the start-codon-set list element).
The same ORF detected under multiple parameter combinations
appears once per combination.
}
\description{
Wraps \code{\link[=scan_orfs]{scan_orfs()}} to enumerate ORFs under combinations of genetic
codes, minimum ORF lengths, and start-codon sets.  Useful for
surveying the full ORFeome of a mitogenome (or partial genome)
under different translation and detection assumptions.
}
\details{
This function is intended for exploratory ORFeome characterization
— for example, "show me every minus-strand ORF $\geq$ 60 nt in
MT-RNR2 under both mitochondrial and standard codes."  When the
goal is a single canonical ORF set, prefer \code{\link[=scan_orfs]{scan_orfs()}} directly.
}
\examples{
\dontrun{
# Pull rCRS and survey its ORFeome under multiple settings
orfs <- scan_orfeome(
  "NC_012920.1",
  genetic_codes    = c("SGC1", "SGC0"),
  min_orf_lengths  = c(60L, 90L)
)
table(orfs$genetic_code, orfs$min_orf_length)
}

}
\seealso{
\code{\link[=scan_orfs]{scan_orfs()}}, \code{\link[=fetch_sequences]{fetch_sequences()}}
}
