% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_orfs.R
\name{scan_orfs}
\alias{scan_orfs}
\title{Scan Multiple Sequences for Open Reading Frames}
\usage{
scan_orfs(
  sequences,
  min_orf_length = 100L,
  max_orf_length = Inf,
  genetic_code = "SGC1",
  start_codons = c("ATG", "GTG", "ATT", "ATC", "ATA"),
  both_strands = TRUE,
  circular = TRUE,
  include_stop_codon = TRUE
)
}
\arguments{
\item{sequences}{A named \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} or named character
vector of nucleotide sequences.  Typically the output of
\code{\link[=fetch_sequences]{fetch_sequences()}}.}

\item{min_orf_length}{Integer. Minimum ORF length in nucleotides (start
codon to stop codon, inclusive).  Default \code{100L}, matching the NCBI ORF
Finder default.}

\item{max_orf_length}{Integer or \code{Inf}. Maximum ORF length in nucleotides.
Default \code{Inf} (no upper limit).  Set a value to focus on the small ORFs
characteristic of mitochondrial-derived peptides (e.g. \code{max_orf_length = 900L} for peptides up to 300 aa).}

\item{genetic_code}{Character scalar. Biostrings genetic-code identifier
passed to \code{\link[=find_orfs]{find_orfs()}}.  Default \code{"SGC1"} (vertebrate mitochondrial
code).}

\item{start_codons}{Character vector of recognised start codons passed to
\code{\link[=find_orfs]{find_orfs()}}.  Default \code{c("ATG", "GTG", "ATT", "ATC", "ATA")}.}

\item{both_strands}{Logical.  Search the reverse-complement strand?
Default \code{TRUE}.}

\item{circular}{Logical.  Treat each sequence as a circular molecule?
Default \code{TRUE}.}

\item{include_stop_codon}{Logical.  Include the stop codon in reported
sequences and lengths?  Default \code{TRUE}.}
}
\value{
A \code{data.frame} with the same columns as \code{\link[=find_orfs]{find_orfs()}} plus a
leading \code{seq_id} column identifying the source sequence.  Rows are
ordered by \code{seq_id} then genomic \code{start} position.  Returns an empty
\code{data.frame} invisibly (with a message) if no ORFs are found.
}
\description{
Applies \code{\link[=find_orfs]{find_orfs()}} to every sequence in a \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} and
returns a single combined \code{data.frame} with a leading \code{seq_id} column.
Designed to accept output directly from \code{\link[=fetch_sequences]{fetch_sequences()}}.
}
\examples{
\dontrun{
# Fetch two human and chimp mitogenomes, then scan for small ORFs
seqs <- fetch_sequences(c("NC_012920.1", "NC_001643.1"))
orfs <- scan_orfs(seqs, min_orf_length = 60, max_orf_length = 900)

# Summarise hits per sequence and strand
table(orfs$seq_id, orfs$strand)

# Restrict to ATG-only, standard code
orfs_std <- scan_orfs(seqs,
                       genetic_code = "SGC0",
                       start_codons = "ATG")
}

}
\seealso{
\code{\link[=find_orfs]{find_orfs()}}, \code{\link[=fetch_sequences]{fetch_sequences()}}
}
