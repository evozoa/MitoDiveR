% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_mdp_similarity.R
\name{score_mdp_similarity}
\alias{score_mdp_similarity}
\title{Score ORF Protein Sequences Against Known Mitochondrial-Derived Peptides}
\usage{
score_mdp_similarity(orfs, min_identity = 20, custom_mdps = NULL)
}
\arguments{
\item{orfs}{\code{data.frame} from \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} or \code{\link[=scan_orfs]{scan_orfs()}}.
Must contain a \code{protein_sequence} column.  If a \code{cluster_id} column is
present, one representative per cluster (the longest sequence) is scored.}

\item{min_identity}{Numeric (0–100).  Minimum percent identity to report a
hit.  Default \code{20} (permissive, to capture distant homologues).}

\item{custom_mdps}{Named character vector of additional reference sequences
to include alongside the built-in MDP set.  Names are used as MDP
identifiers.  \code{NULL} to use only the built-in references.}
}
\value{
A \code{data.frame} with one row per query–MDP pair that meets
\code{min_identity}, with columns:
\describe{
\item{\code{query_id}}{Identifier for the query ORF (cluster_id + seq_id, or
row index).}
\item{\code{cluster_id}}{Cluster identifier, if present in input.}
\item{\code{query_length_aa}}{Length of the query protein (aa).}
\item{\code{query_protein_sequence}}{Full amino acid sequence of the query ORF,
translated using the genetic code selected in \code{\link[=scan_orfs]{scan_orfs()}}.}
\item{\code{mdp_name}}{Name of the matching reference MDP.}
\item{\code{mdp_length_aa}}{Length of the reference MDP (aa).}
\item{\code{mdp_sequence}}{Amino acid sequence of the reference MDP.}
\item{\code{pct_identity}}{Percent identity over the alignment
(matches / alignment length).}
\item{\code{alignment_score}}{Raw Smith-Waterman alignment score.}
\item{\code{query_coverage}}{Fraction of query covered by the alignment (\%).}
\item{\code{mdp_coverage}}{Fraction of the reference MDP covered by the
alignment (\%).}
}
Sorted by \code{cluster_id} then \code{pct_identity} (descending).  Returns an empty
\code{data.frame} invisibly (with a message) if no matches meet \code{min_identity}.
}
\description{
Aligns protein sequences from a \code{\link[=find_conserved_orfs]{find_conserved_orfs()}} (or \code{\link[=scan_orfs]{scan_orfs()}})
result against a curated reference set of known mitochondrial-derived
peptides (MDPs) using local Smith-Waterman alignment.  Returns the best
MDP match per query sequence, enabling rapid prioritisation of candidate
novel MDPs by similarity to known ones.
}
\details{
\subsection{Built-in MDP reference set}{

The following human MDPs are included: Humanin, MOTS-c and SHLP1–SHLP6.  Each
is the UniProt/Swiss-Prot sequence and has been verified to be encoded in the
human mitochondrial reference (rCRS) under the standard genetic code.  Synthetic
analogues (e.g. the S14G humanin variant HNG) are not included, since they are
not encoded in any genome.  Supply additional sequences via \code{custom_mdps} to extend the
reference, for example with species-specific variants or newly published
MDPs.
}

\subsection{No internet required}{

All alignments are performed locally using \code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}}
with BLOSUM62 and Smith-Waterman local alignment.
}
}
\examples{
\dontrun{
seqs      <- fetch_sequences(c("NC_012920.1", "NC_001643.1"))
orfs      <- scan_orfs(seqs, min_orf_length = 60, max_orf_length = 900)
conserved <- find_conserved_orfs(orfs, min_identity = 50)
hits      <- score_mdp_similarity(conserved)
hits[hits$pct_identity >= 40, ]

# Add a custom reference peptide
hits <- score_mdp_similarity(conserved,
          custom_mdps = c(MyPeptide = "MKLLFVIIAVLSACAGAVS"))
}

}
\seealso{
\code{\link[=find_conserved_orfs]{find_conserved_orfs()}}, \code{\link[=blast_orfs]{blast_orfs()}}
}
