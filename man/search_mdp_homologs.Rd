% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_mdp_homologs.R
\name{search_mdp_homologs}
\alias{search_mdp_homologs}
\title{Screen Conserved ORFs for Homology to Mitochondria-Derived Peptides}
\usage{
search_mdp_homologs(
  conserved_orfs,
  min_pct_identity = 20,
  min_coverage = 0,
  min_aligned_aa = 8L,
  mdps = character(0L),
  substitution_matrix = "BLOSUM62",
  gap_opening = 10,
  gap_extension = 4
)
}
\arguments{
\item{conserved_orfs}{A \code{data.frame} — the output of
\code{\link[=cluster_conserved_orfs]{cluster_conserved_orfs()}}.  Must contain \code{cluster_id}, \code{seq_id},
\code{length_aa}, and \code{protein_sequence} columns.}

\item{min_pct_identity}{Numeric.  Minimum percent identity to report a hit.
Default \code{20}.}

\item{min_coverage}{Numeric.  Minimum \code{query_coverage} (0–100) to report
a hit.  Default \code{0} (no filter).}

\item{min_aligned_aa}{Integer.  Minimum number of aligned amino acid
residues (non-gap positions in the MDP alignment) required to report a
hit.  Prevents spurious 100 \%\% identity matches over trivially short
windows.  Default \code{8L}.}

\item{mdps}{Named character vector or list of additional MDP sequences to
include.  These are appended to (not replacing) the built-in library.
Set to \code{character(0)} to use built-in MDPs only (default).}

\item{substitution_matrix}{Character.  Default \code{"BLOSUM62"}.}

\item{gap_opening}{Numeric.  Default \code{10}.}

\item{gap_extension}{Numeric.  Default \code{4}.}
}
\value{
A \code{data.frame} with one row per (cluster representative, MDP) pair
that meets the thresholds, with columns:
\describe{
\item{\code{query_id}}{\verb{cluster_\{cluster_id\}_\{seq_id\}} of the representative.}
\item{\code{cluster_id}}{Integer cluster identifier.}
\item{\code{query_length_aa}}{Length of the representative protein (without stop codon).}
\item{\code{query_protein_sequence}}{Representative protein sequence (without \code{*}).}
\item{\code{mdp_name}}{MDP name.}
\item{\code{mdp_length_aa}}{MDP length in amino acids.}
\item{\code{mdp_sequence}}{MDP sequence.}
\item{\code{pct_identity}}{Percent identity of the local alignment (PID1: matches / alignment length × 100), rounded to 1 decimal.}
\item{\code{alignment_score}}{Raw Smith–Waterman alignment score, rounded to 1 decimal.}
\item{\code{query_coverage}}{Percentage of query ORF covered by the alignment, rounded to 1 decimal.}
\item{\code{mdp_coverage}}{Percentage of MDP covered by the alignment, rounded to 1 decimal.}
}

Returns an empty \code{data.frame} (invisibly) when no hits are found.
}
\description{
Compares a representative protein sequence from each conserved ORF cluster
against a built-in library of known mitochondria-derived peptides (MDPs)
using Smith–Waterman local alignment.  Returns all pairwise comparisons
meeting the minimum identity threshold.
}
\details{
\subsection{Built-in MDP library}{

The natural peptides below are the UniProt/Swiss-Prot sequences, each verified
to be encoded in the human mitochondrial reference (rCRS, NC_012920.1) and
translated with the standard genetic code, because MDPs are made by
cytoplasmic ribosomes. \code{tests/testthat/test-mdp_references.R} re-derives every
one of them from the rCRS coding sequence.\tabular{llll}{
   Name \tab Length (aa) \tab UniProt \tab Location in rCRS \cr
   Humanin \tab 24 \tab Q8IVG9 \tab MT-RNR2 2633-2704 (+) \cr
   MOTS-c \tab 16 \tab A0A0C5B5G6 \tab MT-RNR1 1343-1390 (+) \cr
   SHLP1 \tab 24 \tab A0A3G1DJL7 \tab MT-RNR2 2488-2559 (-) \cr
   SHLP2 \tab 26 \tab A0A3G1DIU6 \tab MT-RNR2 2091-2168 (-) \cr
   SHLP3 \tab 38 \tab A0A3G1DJQ2 \tab MT-RNR2 1706-1819 (-) \cr
   SHLP4 \tab 26 \tab A0A3G1DJK2 \tab MT-RNR2 2445-2522 (-) \cr
   SHLP5 \tab 24 \tab A0A3G1DJL1 \tab MT-RNR2 2783-2854 (-) \cr
   SHLP6 \tab 20 \tab A0A3G1DJN1 \tab MT-RNR2 2990-3049 (+) \cr
}


Synthetic analogues (e.g. the S14G humanin variant, HNG) are deliberately
absent: they are not encoded in any genome, so they cannot be recovered by a
genome screen. Supply \code{mdps} to add custom sequences or replace defaults.

Note: before 2026-08, the built-in SHLP1-6 entries were sequences that are not
encoded anywhere in the human mitochondrial genome; screens run with them could
not have recovered a real SHLP. They were corrected to the Swiss-Prot sequences.
}

\subsection{Cluster representative}{

One sequence per cluster is selected: the longest protein (stop-codon
asterisk stripped), with ties broken by \code{seq_id} alphabetically.  The
\code{query_id} column reports \verb{cluster_\{id\}_\{seq_id\}} of the representative.
}

\subsection{Coverage metrics}{
\itemize{
\item \code{query_coverage}: percentage of the \emph{query ORF} covered by the
alignment window (alignment width / query length × 100).
\item \code{mdp_coverage}: percentage of the \emph{MDP} covered by the alignment
window (alignment width / MDP length × 100).  Values > 100 indicate
that the alignment window (including gap columns) is wider than the MDP.
}
}
}
\examples{
\dontrun{
conserved <- cluster_conserved_orfs(orfs_all)
hits <- search_mdp_homologs(conserved)

# Best hit per cluster
hits[order(hits$cluster_id, -hits$pct_identity), ]

# Add a custom peptide
hits2 <- search_mdp_homologs(conserved,
                              mdps = c(MyPeptide = "MLLSALVGAAVAFVLA"))
}

}
