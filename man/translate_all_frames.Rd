% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translate_all_frames.R
\name{translate_all_frames}
\alias{translate_all_frames}
\title{Translate a Mitochondrial Genome in All Six Reading Frames}
\usage{
translate_all_frames(
  genome_record,
  genetic_codes = .stop_no_code("genetic_codes")
)
}
\arguments{
\item{genome_record}{A single genome record — one element of the list
returned by \code{\link[=fetch_mito_genbank]{fetch_mito_genbank()}}.  Must contain \verb{$sequence}
(a \link[Biostrings:DNAString-class]{Biostrings::DNAString}) and \verb{$accession}.}

\item{genetic_codes}{Character vector of Biostrings genetic-code
identifiers passed to \code{\link[Biostrings:GENETIC_CODE]{Biostrings::getGeneticCode()}}.
Required (no default) — e.g. \code{c("SGC0", "SGC1")} to translate under both
the standard vertebrate nuclear code and the vertebrate mitochondrial code,
yielding 12 strings total.}
}
\value{
A named list of \link[Biostrings:AAString-class]{Biostrings::AAString} objects.  Names follow the
pattern \verb{\{accession\}_\{code\}_\{strand\}\{frame\}}, e.g.
\code{NC_002333.2_SGC0_+1}, \code{NC_002333.2_SGC1_-3}.  Stop codons are
represented as \code{X}.
}
\description{
Translates a mitogenome sequence in all three forward and three
reverse-complement reading frames, optionally under multiple genetic codes.
Stop codons are replaced with \code{X} so that the resulting strings can be used
as continuous amino acid sequences for window-based homology searches with
\code{\link[=find_conserved_windows]{find_conserved_windows()}}.
}
\details{
Each reading frame is translated as a single uninterrupted string — stop
codons do \strong{not} terminate translation.  This preserves the full-length
context needed for inter-species window comparison and allows
\code{\link[=find_conserved_windows]{find_conserved_windows()}} to detect conserved regions that span or
adjoin canonical stop codons in one species but not another.

Reverse-complement frames are numbered \code{-1}, \code{-2}, \code{-3} following the
same convention as \code{\link[=find_orfs]{find_orfs()}}: \code{-1} starts from the last nucleotide
of the genome, \code{-2} from the second-to-last, etc.
}
\examples{
\dontrun{
genomes <- fetch_mito_genbank("NC_002333.2")
tr <- translate_all_frames(genomes[["NC_002333.2"]])
names(tr)
nchar(tr[["NC_002333.2_SGC0_+1"]])
}

}
